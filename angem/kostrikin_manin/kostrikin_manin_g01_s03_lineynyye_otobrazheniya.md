## 1.3. Линейные отображения

---
**стр. 21**
---

же рассуждение, как при доказательстве утверждения б) леммы п. 9, показывает тогда, что $l$ есть (конечная) линейная комбинация элементов $Y$, т. е. $Y$ образует базис в $L$.

## Упражнения

1. Пусть $L$ — пространство многочленов от $x$ степени $\leqslant n-1$ с коэффициентами в поле $\mathscr{K}$. Проверить следующие утверждения:

   a) $1, x, \ldots, x^{n-1}$ образуют базис в $L$. Координаты многочлена $f$ в этом базисе — это его коэффициенты.

   б) $1, x-a, (x-a)^2, \ldots, (x-a)^{n-1}$ образуют базис в $L$. Если $\operatorname{char} \mathscr{K} = p \geqslant n$, то координаты многочлена $f$ в этом базисе: $\left\{f(a), f'(a), \frac{f''(a)}{2!}, \ldots, \frac{f^{(n-1)}(a)}{(n-1)!}\right\}$.

   в) Пусть $a_1, \ldots, a_n \in \mathscr{K}$ — попарно различные элементы. Положим $g_i(x) = \prod\limits_{j \neq i} (x-a_j)(a_i-a_j)^{-1}$. Многочлены $g_1(x), \ldots, g_n(x)$ образуют базис $L$ («интерполяционный базис»). Координаты многочлена $f$ в этом базисе: $\{f(a_1), \ldots, f(a_n)\}$.

2. Пусть $L$ — $n$-мерное пространство, $f\colon L \to \mathscr{K}$ — ненулевой линейный функционал. Доказать, что $M = \{l \in L \mid f(l) = 0\}$ является $(n-1)$-мерным подпространством в $L$. Доказать, что все $(n-1)$-мерные подпространства получаются таким способом.

3. Пусть $L$ — $n$-мерное пространство, $M \subset L$ — $m$-мерное подпространство. Доказать, что существуют линейные функционалы $f_1, \ldots, f_{n-m} \in L^*$ такие, что $M = \{l \mid f_1(l) = \ldots = f_{n-m}(l) = 0\}$.

4. Вычислить размерности следующих пространств:

   a) пространства многочленов степени $\leqslant p$ от $n$ переменных;

   б) пространства однородных многочленов (форм) степени $p$ от $n$ переменных;

   в) пространства функций из $F(S)$, $|S| < \infty$, обращающихся в нуль во всех точках из подмножества $S_0 \subset S$.

5. Пусть $\mathscr{K}$ — конечное поле характеристики $p$. Доказать, что число его элементов равно $p^n$ для некоторого $n \geqslant 1$. (*Указание.* Рассмотреть $\mathscr{K}$ как линейное пространство над простым подполем, состоящим из всех «сумм единиц» в $\mathscr{K}$: $0, 1, 1+1, \ldots$)

6. Заменой понятия флага в бесконечномерном случае служит понятие цепи подпространств (упорядоченных по включению). Пользуясь леммой Цорна, доказать, что всякая цепь содержится в максимальной.

## § 3. Линейные отображения

**1. Определение.** *Пусть $L$, $M$ — линейные пространства над полем $\mathscr{K}$. Отображение $f\colon L \to M$ называется линейным, если для всех $l, l_1, l_2 \in L$, $a \in \mathscr{K}$ имеем*

$$f(al) = af(l),\quad f(l_1+l_2) = f(l_1)+f(l_2).$$

Линейное отображение является гомоморфизмом аддитивных групп. В самом деле, $f(0) = 0f(0) = 0$ и $f(-l) = f((-1)l) = -f(l)$. Индукция по $n$ показывает, что для любых $a_i \in \mathscr{K}$, $l_i \in L$ имеем $f\left(\sum\limits_{i=1}^n a_i l_i\right) = \sum\limits_{i=1}^n a_i f(l_i)$.

Линейные отображения $f\colon L \to L$ называются также *линейными операторами* на $L$.

---
**стр. 22**
---

**2. Примеры.** а) *Нулевое* линейное отображение $f\colon L \to M$, $f(l) = 0$ для всех $l \in L$. *Тождественное* линейное отображение: $f\colon L \to L$, $f(l) = l$ для всех $l \in L$. Оно обозначается $\operatorname{id}_L$ или $\operatorname{id}$ (от английского слова «identity»). *Умножение на скаляр* $a \in \mathscr{K}$, или *гомотетия* $f\colon L \to L$, $f(l) = al$ для всех $l \in L$. При $a = 0$ получается нулевой оператор, при $a = 1$ — тождественный.

б) Линейные отображения $f\colon L \to \mathscr{K}$ — это линейные функции, или функционалы, на $L$ (см. § 1, п. 9). Пусть $L$ — пространство с базисом $\{e_1, \ldots, e_n\}$. Для любого $1 \leqslant i \leqslant n$ отображение $e^i\colon L \to \mathscr{K}$, где $e^i(l)$ — $i$-я координата $l$ в базисе $\{e_1, \ldots, e_n\}$, является линейным функционалом.

в) Пусть $L = \{x \in \mathbf{R} \mid x > 0\}$ наделено структурой линейного пространства над $\mathbf{R}$, описанной в § 1, пример а) п. 10, $M = \mathbf{R}^1$. Отображение $\log\colon L \to M$, $x \mapsto \log x$, $\mathbf{R}$-линейно.

г) Пусть $S \subset T$ — два множества. Отображение $F(T) \to F(S)$, которое всякой функции на $T$ ставит в соответствие ее ограничение на $S$, линейно. В частности, если $S = \{s\}$, $s \in T$, $f \in F(T)$, то отображение: $f \mapsto (\text{значение } f \text{ в точке } s)$ линейно.

Конструкция линейных отображений с нужными свойствами часто основывается на следующем результате.

**3. Предложение.** *Пусть $L$, $M$ — линейные пространства над полем $\mathscr{K}$; $\{l_1, \ldots, l_n\} \subset L$ и $\{m_1, \ldots, m_n\} \subset M$ — два семейства векторов с одинаковым числом элементов. Тогда:*

   a) *если линейная оболочка $\{l_1, \ldots, l_n\}$ совпадает с $L$, то существует не больше одного линейного отображения $f\colon L \to M$, для которого $f(l_i) = m_i$ при всех $i$;*

   б) *если $\{l_1, \ldots, l_n\}$ к тому же линейно независимы, т. е. образуют базис $L$, то такое отображение существует.*

Доказательство. Пусть $f$, $f'$ — пара отображений с $f(l_i) = f'(l_i) = m_i$ для всех $i$. Рассмотрим отображение $g = f - f'$, где $(f-f')(l) = f(l) - f'(l)$. Легко проверить, что оно линейно. Кроме того, оно переводит в нуль все $l_i$ и потому любую линейную комбинацию векторов $l_i$. Значит, $f$ и $f'$ совпадают на каждом векторе из $L$, откуда $f' = f$.

Пусть теперь $\{l_1, \ldots, l_n\}$ образует базис $L$. Так как каждый элемент $L$ однозначно представляется в виде $\sum\limits_{i=1}^n a_i l_i$, мы можем определить теоретико-множественное отображение $f\colon L \to M$ формулой

$$f\left(\sum_{i=1}^n a_i l_i\right) = \sum_{i=1}^n a_i m_i.$$

Его линейность проверяется непосредственно.

В этом доказательстве использовалась разность двух линейных отображений $L \to M$. Это частный случай следующей более общей конструкции.

**4.** Обозначим через $\mathscr{L}(L, M)$ множество линейных отображений из $L$ в $M$. Для $f, g \in \mathscr{L}(L, M)$ и $a \in \mathscr{K}$ определим $af$ и $f+g$

---
**стр. 23**
---

формулами

$$(af)(l) = a(f(l)),\quad (f+g)(l) = f(l) + g(l)$$

для всех $l \in L$. Точно так же, как в § 1, п. 9, проверяется, что $af$ и $f+g$ линейны, так что $\mathscr{L}(L, M)$ — линейное пространство.

**5.** Пусть $f \in \mathscr{L}(L, M)$ и $g \in \mathscr{L}(M, N)$. Теоретико-множественная композиция $g \circ f = gf\colon L \to N$ является линейным отображением. Действительно,

$$(gf)(l_1+l_2) = g[f(l_1+l_2)] = g[f(l_1)+f(l_2)] = g[f(l_1)] + g[f(l_2)] = gf(l_1) + gf(l_2)$$

и, аналогично, $(gf)(al) = a(gf(l))$.

Очевидно, $\operatorname{id}_M \circ f = f \circ \operatorname{id}_L = f$. Кроме того, $h(gf) = (hg)f$, когда обе части определены, так что скобки можно опустить; это общее свойство ассоциативности теоретико-множественных отображений. Наконец, композиция $gf$ линейна по каждому из аргументов при фиксированном втором: например, $g \circ (af_1+bf_2) = a(g \circ f_1) + b(g \circ f_2)$.

**6.** Пусть $f \in \mathscr{L}(L, M)$ — биективное отображение. Тогда у него есть теоретико-множественное обратное отображение $f^{-1}\colon M \to L$. Мы утверждаем, что $f^{-1}$ автоматически линейно. Для этого следует проверить, что

$$f^{-1}(m_1+m_2) = f^{-1}(m_1) + f^{-1}(m_2),\quad f^{-1}(am_1) = af^{-1}(m_1)$$

для всех $m_1, m_2 \in M$; $a \in \mathscr{K}$. Поскольку $f$ биективно, существуют и однозначно определены такие векторы $l_1, l_2 \in L$, что $m_i = f(l_i)$. Написав формулы

$$f(l_1)+f(l_2) = f(l_1+l_2),\quad af(l_1) = f(al_1),$$

применив к их обеим частям $f^{-1}$ и заменив в результате $l_i$ на $f^{-1}(m_i)$, получим требуемое.

Биективные линейные отображения $f\colon L \to M$ называются *изоморфизмами*. Пространства $L$ и $M$ называются *изоморфными*, если между ними существует изоморфизм.

Следующая теорема показывает, что размерность пространства полностью определяет его с точностью до изоморфизма.

**7. Теорема.** *Два конечномерных пространства $L$ и $M$ над полем $\mathscr{K}$ изоморфны тогда и только тогда, когда у них одинаковые размерности.*

Доказательство. Изоморфизм $l\colon L \to M$ сохраняет все свойства, формулируемые в терминах линейных комбинаций. В частности, он переводит любой базис $L$ в некоторый базис $M$, так что размерности $L$ и $M$ совпадают. (Из этого рассуждения следует также, что конечномерное пространство не может быть изоморфно бесконечномерному.)

Наоборот, пусть размерности $L$ и $M$ равны $n$. Выберем базисы $\{l_1, \ldots, l_n\}$ и $\{m_1, \ldots, m_n\}$ в $L$ и $M$ соответственно. Формула

$$f\left(\sum_{i=1}^n a_i l_i\right) = \sum_{i=1}^n a_i m_i$$

---
**стр. 24**
---

определяет линейное отображение $L$ в $M$ по предложению п. 3. Оно является биекцией, ибо формула

$$f^{-1}\left(\sum_{i=1}^n a_i m_i\right) = \sum_{i=1}^n a_i l_i$$

определяет обратное линейное отображение $f^{-1}$.

**8. Предупреждение.** Если даже изоморфизм между двумя линейными пространствами $L, M$ и существует, он определен однозначно только в двух случаях:

   a) $L = M = \{0\}$,

   б) $L$ и $M$ одномерны, а $\mathscr{K}$ — поле из двух элементов (попробуйте доказать это!).

Во всех остальных случаях имеется много (если $\mathscr{K}$ бесконечно, то бесконечно много) изоморфизмов. В частности, имеется много изоморфизмов пространства $L$ с самим собой. В силу результатов пп. 5 и 6 они образуют группу относительно теоретико-множественной композиции. Эта группа называется *полной линейной группой пространства* $L$. Позже мы сможем описать ее в более явном виде как группу невырожденных квадратных матриц.

Иногда бывает, что между двумя линейными пространствами определен некоторый изоморфизм, не зависящий ни от каких произвольных выборов (как выборы базисов в пространствах $L$ и $M$ в доказательстве теоремы п. 7). Такие изоморфизмы мы будем называть *каноническими* или *естественными* (точное определение этих терминов можно дать только на категорном языке, о котором см. § 13). Следует тщательно отличать естественные изоморфизмы от «случайных». Мы приведем два характерных примера, очень важных для понимания этого различия.

**9. «Случайный» изоморфизм между пространством и двойственным к нему.** Пусть $L$ — конечномерное пространство с базисом $\{e_1, \ldots, e_n\}$. Обозначим через $e^i \in L^*$ линейный функционал

$$l \mapsto e^i(l),\text{ где } e^i(l) \text{ — } i\text{-я координата вектора } l \text{ в базисе } \{e_i\}$$

(не путать с $i$-й степенью; в линейном пространстве она не определена). Мы утверждаем, что *функционалы $\{e^1, \ldots, e^n\}$ образуют базис в $L^*$, так называемый двойственный к $\{e_1, \ldots, e_n\}$ базис.* Равносильное описание $\{e^i\}$ такое: $e^i(e_k) = \delta_{ik}$ (символ Кронекера: $1$ при $i = k$, $0$ при $i \neq k$).

В самом деле, всякий линейный функционал $f\colon L \to \mathscr{K}$ можно представить в виде линейной комбинации $\{e^i\}$:

$$f = \sum_{i=1}^n f(e_i)e^i.$$

Действительно, значения левой и правой части совпадают на любой линейной комбинации $\sum\limits_{k=1}^n a_k e_k$, потому что $e^i\left(\sum\limits_{k=1}^n a_k e_k\right) = a_i$ по определению $e^i$,


---
**стр. 25**
---

Кроме того, $\{e_i\}$ линейно независимы: если $\sum\limits_{i=1}^n a_i e^i=0$, то для всех $k$, $1\leqslant k\leqslant n$, имеем $a_k=\left(\sum\limits_{i=1}^n a_i e^i\right)(e_k)=0$.

Поэтому $L$ и $L^*$ имеют одинаковую размерность $n$ и даже определён изоморфизм $f\colon L\to L^*$, который переводит $e_i$ в $e^i$.

Однако *этот изоморфизм не каноничен*: замена базиса $\{e_1,\dots,e_n\}$, вообще говоря, меняет его. Так, если $L$ одномерно, то для любого ненулевого вектора $e_1\in L$ семейство $\{e_1\}$ является базисом $L$. Пусть $\{e^1\}$ — двойственный базис к $\{e_1\}$, $e^1(e_1)=1$. Тогда к базису $\{ae_1\}$, $a\in\mathcal K\setminus\{0\}$, двойствен базис $\{a^{-1}e^1\}$. Но линейные отображения $f_1\colon e_1\mapsto e^1$ и $f_2\colon ae_1\mapsto a^{-1}e^1$ различны, если только $a^2\neq1$.

**10. Канонический изоморфизм между пространством и дважды двойственным к нему.** Пусть $L$ — линейное пространство, $L^*$ — пространство линейных функций на нём, $L^{**}=(L^*)^*$ — пространство линейных функций на $L^*$ — «дважды двойственное к $L$ пространство».

Опишем каноническое отображение $\varepsilon_L\colon L\to L^{**}$, не зависящее ни от каких произвольных выборов. *Оно ставит в соответствие каждому вектору $l\in L$ функцию на $L^*$, значение которой на функционале $f\in L^*$ равно $f(l)$;* в краткой записи:

$$\varepsilon_L\colon\quad l\mapsto[f\mapsto f(l)].$$

Проверим следующие свойства $\varepsilon_L$:

а) *Для каждого $l\in L$ отображение $\varepsilon_L(l)\colon L^*\to\mathcal K$ линейно.* Действительно, это означает, что *выражение $f(l)$ как функция от $f$ при фиксированном $l$ линейно по $f$.* Но это следует из правил сложения функционалов и умножения их на скаляр (§ 1, п. 7).

Следовательно, $\varepsilon_L$ действительно определяет отображение $L$ в $L^{**}$, как и утверждалось.

б) *Отображение $\varepsilon_L\colon L\to L^{**}$ линейно.* Действительно, это означает, что *выражение $f(l)$ как функция от $l$ при фиксированном $f$ линейно,* — это так, ибо $f\in L^*$.

в) *Если $L$ конечномерно, то отображение $\varepsilon_L\colon L\to L^{**}$ является изоморфизмом.* В самом деле, пусть $\{e_1,\dots,e_n\}$ — базис $L$, $\{e^1,\dots,e^n\}$ — двойственный базис $L^*$, $\{e'_1,\dots,e'_n\}$ — базис в $L^{**}$, двойственный к $\{e^1,\dots,e^n\}$.

Покажем, что $\varepsilon_L(e_i)=e'_i$, откуда и будет следовать, что $\varepsilon_L$ — изоморфизм (в этой проверке использование базиса $L$ безобидно, ибо в определении $\varepsilon_L$ он не участвовал!).

В самом деле, $\varepsilon_L(e_i)$ согласно определению есть функционал на $L^*$, значение которого на $e^k$ равно $e^k(e_i)=\delta_{ik}$ («символ Кронекера»). Но $e'_i$ — точно такой же функционал на $L^*$ по определению двойственного базиса.

Заметим, что если $L$ бесконечномерно, то $\varepsilon_L\colon L\to L^{**}$ остаётся инъективным, но перестаёт быть сюръективным (см. упражнение 2). В функциональном анализе вместо полного $L^*$ обычно рассмат-

---
**стр. 26**
---

ривают только подпространство линейных функционалов $L'$, *непрерывных в подходящей топологии* на $L$ и $\mathcal K$, и тогда отображение $L\to L''$ может быть определено и иногда оказывается изоморфизмом. Такие (топологические) пространства называют *рефлексивными.* Мы доказали, что *конечномерные пространства* (без учёта топологии) *рефлексивны.*

Рассмотрим теперь связь между линейными отображениями и линейными подпространствами.

**11. Определение.** *Пусть $f\colon L\to M$ — линейное отображение. Множество $\operatorname{Ker}f=\{l\in L\mid f(l)=0\}\subset L$ называется ядром $f$, а множество $\operatorname{Im}f=\{m\in M\mid\exists\,l\in L,\,f(l)=m\}\subset M$ называется образом $f$.*

Нетрудно убедиться, что ядро $f$ является линейным подпространством в $L$, а образ $f$ — линейным подпространством в $M$. Проверим, например, второе утверждение. Пусть $m_1,m_2\in\operatorname{Im}f$, $a\in\mathcal K$. Тогда существуют такие векторы $l_1,l_2\in L$, что $f(l_1)=m_1$, $f(l_2)=m_2$. Значит, $m_1+m_2=f(l_1+l_2)$, $am_1=f(al_1)$. Следовательно, $m_1+m_2\in\operatorname{Im}f$ и $am_1\in\operatorname{Im}f$.

Отображение $f$ инъективно тогда и только тогда, когда $\operatorname{Ker}f=\{0\}$. В самом деле, если $f(l_1)=f(l_2)$, $l_1\neq l_2$, то $0\neq l_1-l_2\in\operatorname{Ker}f$. Наоборот, если $0\neq l\in\operatorname{Ker}f$, то $f(l)=0=f(0)$.

**12. Теорема.** *Пусть $L$ — конечномерное линейное пространство, $f\colon L\to M$ — линейное отображение. Тогда $\operatorname{Ker}f$ и $\operatorname{Im}f$ конечномерны и*

$$\dim\operatorname{Ker}f+\dim\operatorname{Im}f=\dim L.$$

*Доказательство.* Ядро $f$ конечномерно по следствию п. 13, § 2. Выберем базис $\{e_1,\dots,e_m\}$ в $\operatorname{Ker}f$ и продолжим его до базиса $\{e_1,\dots,e_m,e_{m+1},\dots,e_{m+n}\}$ пространства $L$ по теореме п. 12, § 2. Покажем, что векторы $f(e_{m+1}),\dots,f(e_{m+n})$ образуют базис в $\operatorname{Im}f$. Отсюда, очевидно, будет следовать теорема.

Любой вектор из $\operatorname{Im}f$ имеет вид

$$f\left(\sum_{i=1}^{m+n}a_ie_i\right)=\sum_{i=m+1}^{m+n}a_if(e_i).$$

Следовательно, $f(e_{m+1}),\dots,f(e_{m+n})$ порождают $\operatorname{Im}f$.

Предположим, что $\sum\limits_{i=m+1}^{m+n}a_if(e_i)=0$. Тогда $f\left(\sum\limits_{i=m+1}^{m+n}a_ie_i\right)=0$.
Это значит, что $\sum\limits_{i=m+1}^{m+n}a_ie_i\in\operatorname{Ker}f$, т. е. $\sum\limits_{i=m+1}^{m+n}a_ie_i=\sum\limits_{j=1}^m a_je_j$. Это возможно, только если все коэффициенты равны нулю, ибо $\{e_1,\dots,e_{m+n}\}$ — базис $L$. Следовательно, векторы $f(e_{m+1}),\dots,f(e_{m+n})$ линейно независимы. Теорема доказана.

**13. Следствие.** *Следующие свойства $f$ равносильны (в случае конечномерного $L$):*

а) $f$ инъективно.

б) $\dim L=\dim\operatorname{Im}f$.

*Доказательство.* Согласно теореме, $\dim L=\dim\operatorname{Im}f$ тогда и только тогда, когда $\dim\operatorname{Ker}f=0$, т. е. $\operatorname{Ker}f=\{0\}$.
