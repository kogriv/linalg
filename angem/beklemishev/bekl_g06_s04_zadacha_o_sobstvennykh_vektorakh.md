# Глава VI. Линейные пространства

## § 4. Задача о собственных векторах

**1. Линейные преобразования.** Линейное преобразование — это отображение, которое отображает линейное пространство в то же самое пространство. В этом параграфе мы будем заниматься исключительно преобразованиями. Все результаты об отображениях верны и для преобразований, но здесь должны быть сделаны существенные оговорки, касающиеся координатной записи преобразования.

Именно, для координатной записи отображения $\mathsf{A}:\mathcal{L}\to\overline{\mathcal{L}}$ выбираются базисы в обоих пространствах $\mathcal{L}$ и $\overline{\mathcal{L}}$. Если же пространства $\mathcal{L}$ и $\overline{\mathcal{L}}$ совпадают, естественно пользоваться одним и тем же базисом и для векторов и для их образов. Поэтому вводится следующее

О п р е д е л е н и е. *Матрицей линейного преобразования* $\mathsf{A}:\mathcal{L}\to\mathcal{L}$ в базисе $\mathbf{e}=e_1,\ldots,e_n$ называется матрица, столбцы которой — координатные столбцы векторов $\mathsf{A}(e_1),\ldots,\mathsf{A}(e_n)$ в базисе $\mathbf{e}$.

В соответствии с этим определением, формула (6) § 3 для матрицы преобразования принимает вид

$$A'=S^{-1}AS. \tag{1}$$

Множество матриц $A'$, получаемых из данной матрицы $A$ по формуле (1), уже, чем множество матриц, получаемых из той же матрицы $A$ по формуле (6) § 3 при не связанных между собой матрицах $S$ и $P$. В более узком множестве, вообще говоря, не найдется матрицы канонического вида (7) § 3 и теорема 2 § 3 не верна для преобразований.

Не следует думать, что это — случайное обстоятельство, связанное с неудачным определением матрицы преобразования. Матрица отображения задает это отображение, и потому все свойства отображения содержатся среди свойств его матрицы. Свойствами отображения являются те свойства его матрицы, которые инвариантны, то есть не меняются при переходе к другой паре базисов, а остальные описывают как бы его расположение по отношению к базисам. Теорема 2 § 3, по существу, означает, что единственным свойством линейного отображения является его ранг.

Линейные преобразования имеют больше свойств, чем линейные отображения. Это связано с тем, что образ вектора лежит в том же пространстве, и мы получаем возможность говорить о взаимном расположении вектора и его образа. Например, приобретают смысл вопросы о том, коллинеарен ли вектор своему образу, имеют ли ядро и множество значений ненулевое пересечение. Для отображения $\mathcal{L}$ в другое пространство $\overline{\mathcal{L}}$ эти вопросы лишены смысла. Естественно, что матрица преобразования должна иметь больше инвариантных свойств, чем матрица отображения, а это означает, что множество матриц, задающих преобразование в различных базисах, должно быть уже, чем соответствующее множество для отображения.

**2. Умножение преобразований.** Линейные преобразования обладают той особенностью, что произведение определено для любых преобразований одного пространства. В частности, если $\mathsf{A}$ и $\mathsf{B}$ — преобразования пространства $\mathcal{L}$, то определены $\mathsf{AB}$ и $\mathsf{BA}$. Эти произведения, вообще говоря, различны. Однако, может случиться, что $\mathsf{AB}=\mathsf{BA}$. В этом случае говорят, что $\mathsf{A}$ и $\mathsf{B}$ *перестановочны* или *коммутируют*.

Произведение $\mathsf{AA}$ естественно обозначить $\mathsf{A}^2$, и определить целую положительную степень $\mathsf{A}$ по индукции соотношением $\mathsf{A}^k=\mathsf{AA}^{k-1}$. Нулевой степенью преобразования по определению считают тождественное преобразование $\mathsf{E}$.

---
**стр. 255**

---

Линейное преобразование $\mathsf{B}$, представленное как линейная комбинация целых неотрицательных степеней преобразования $\mathsf{A}$

$$\mathsf{B}=\alpha_0\mathsf{E}+\alpha_1\mathsf{A}+\cdots+\alpha_k\mathsf{A}^k,$$

называется *многочленом от преобразования* $\mathsf{A}$ или, точнее, значением многочлена $p(t)=\alpha_0+\alpha_1t+\cdots+\alpha_kt^k$ на преобразовании $\mathsf{A}$, и обозначается $p(\mathsf{A})$.

Нетрудно проверить, что любой многочлен от $\mathsf{A}$ перестановочен с $\mathsf{A}$, и что любые два многочлена от $\mathsf{A}$ перестановочны.

Отметим, что при нашем определении матрицы преобразования сохраняется все сказанное о связи алгебраических операций над отображениями с соответствующими операциями над их матрицами. В частности, матрицей произведения $\mathsf{BA}$ преобразований в базисе $\mathbf{e}$ будет произведение $BA$ их матриц, и для произвольного многочлена $p(\mathsf{A})$ матрицей в каком-либо базисе будет матрица $p(A)$.

**3. Инвариантные подпространства.** Рассмотрим линейное пространство $\mathcal{L}$ и его линейное преобразование $\mathsf{A}$.

О п р е д е л е н и е. Подпространство $\mathcal{L}'\subseteq\mathcal{L}$ называется *инвариантным* относительно $\mathsf{A}$, если для каждого вектора $x$ из $\mathcal{L}'$ образ $\mathsf{A}(x)$ лежит в $\mathcal{L}'$, или, что то же, $\mathsf{A}(\mathcal{L}')\subseteq\mathcal{L}'$.

П р и м е р 1. Рассмотрим обычное геометрическое пространство и поворот $\mathsf{A}$ этого пространства на угол $\alpha$ вокруг заданной оси $p$. При повороте вектор переходит в вектор и, следовательно, поворот порождает преобразование трехмерного векторного пространства. Очевидно, что это преобразование линейное. Векторы, лежащие на оси $p$, образуют одномерное инвариантное подпространство, так как для них $\mathsf{A}(x)=x$. Векторы, перпендикулярные оси $p$, образуют двумерное инвариантное подпространство, так как вектор, перпендикулярный оси, после поворота останется ей перпендикулярным.

П р и м е р 2. Нулевое подпространство инвариантно относительно любого преобразования.

П р и м е р 3. Пространство $\mathcal{L}$, рассматриваемое как подпространство, является инвариантным относительно любого преобразования.

П р и м е р 4. Каждое подпространство является инвариантным относительно тождественного и нулевого преобразований.

---
**стр. 256**

---

П р и м е р 5. Ядро преобразования и множество его значений являются его инвариантными подпространствами.

Пусть в $n$-мерном линейном пространстве $\mathcal{L}$ задано линейное преобразование $\mathsf{A}$, и пусть $k$-мерное подпространство $\mathcal{L}'$ инвариантно относительно $\mathsf{A}$. Выберем в $\mathcal{L}$ базис $e_1,\ldots,e_n$ так, чтобы векторы $e_1,\ldots,e_k$ лежали в $\mathcal{L}'$. Матрица $A$ преобразования $\mathsf{A}$ может быть разделена на четыре подматрицы или, как говорят, клетки:

$$A=\begin{Vmatrix}A_1&A_2\\A_3&A_4\end{Vmatrix}.$$

Клетки $A_1$, $A_2$, $A_3$ и $A_4$ имеют размеры $k\times k$, $k\times(n-k)$, $(n-k)\times k$ и $(n-k)\times(n-k)$, соответственно. Докажем, что $A_3=O$, то есть элементы $\alpha^i_j$ матрицы $A$ равны нулю при $j=1,\ldots,k$ и $i=k+1,\ldots,n$. Действительно, первые $k$ столбцов матрицы $A$ — координатные столбцы векторов $\mathsf{A}(e_1),\ldots,\mathsf{A}(e_k)$. Так как $\mathcal{L}'$ инвариантное подпространство, эти векторы лежат в $\mathcal{L}'$, и их компоненты по базисным векторам $e_{k+1},\ldots,e_n$ равны нулю.

Легко видеть, что и обратно, если в каком-либо базисе матрица линейного преобразования $\mathsf{A}$ имеет вид

$$A=\begin{Vmatrix}A_1&A_2\\O&A_4\end{Vmatrix}, \tag{2}$$

то линейная оболочка векторов $e_1,\ldots,e_k$ инвариантна относительно $\mathsf{A}$. В самом деле, в этом случае для всех $j=1,\ldots,k$ имеем $\mathsf{A}(e_j)=\alpha^1_je_1+\cdots+\alpha^k_je_k$, и потому образ линейной комбинации векторов $e_1,\ldots,e_k$ есть линейная комбинация этих же векторов.

Матрицы вида (2) в п. 5 § 4 гл. V мы назвали клеточно-треугольными. Итак,

- Матрица линейного преобразования клеточно-треугольная тогда и только тогда, когда линейная оболочка базисных векторов $e_1,\ldots,e_k$ — инвариантное подпространство.

---
**стр. 257**

---

Преобразование $\mathsf{A}$ каждому вектору из инвариантного подпространства $\mathcal{L}'$ сопоставляет вектор из $\mathcal{L}'$. Этим определено преобразование подпространства $\mathcal{L}'$, которое мы назовем *ограничением* $\mathsf{A}$ на $\mathcal{L}'$ и обозначим $\mathsf{A}'$. Для векторов из $\mathcal{L}'$ по определению $\mathsf{A}'(x)=\mathsf{A}(x)$, а для векторов, не принадлежащих $\mathcal{L}'$, преобразование $\mathsf{A}'$ не определено. $\mathsf{A}'$ отличается от $\mathsf{A}$ только тем, что оно преобразует $\mathcal{L}'$ в $\mathcal{L}'$, а не $\mathcal{L}$ в $\mathcal{L}$.

Если сохранить обозначения, введенные выше, то нетрудно заметить, что в базисе $e_1,\ldots,e_k$ подпространства $\mathcal{L}'$ матрицей ограничения $\mathsf{A}'$ является клетка $A_1$ матрицы (2). Таким образом, имеет место

П р е д л о ж е н и е 1. *Если первые $k$ векторов базиса составляют базис инвариантного подпространства $\mathcal{L}'$, то матрица преобразования имеет вид (2), и клетка $A_1$ — матрица ограничения $\mathsf{A}$ на $\mathcal{L}'$.*

Если мы поместим в инвариантное подпространство не первые $k$ базисных векторов, а базисные векторы с номерами $p+1,\ldots,p+k$ при каком-то $p$, то повторением тех же рассуждений мы получим для элементов матрицы $A$ равенства $\alpha^i_j=0$ при $j=p+1,\ldots,p+k$ и $i<p+1$ или $i>p+k$. Это значит, что в столбцах с номерами $p+1,\ldots,p+k$ может быть отлична от нуля только квадратная клетка порядка $k$ в строках с теми же номерами, то есть расположенная на главной диагонали.

Пусть теперь $\mathcal{L}$ разложено в прямую сумму $s$ инвариантных подпространств $\mathcal{L}=\mathcal{L}_1\oplus\cdots\oplus\mathcal{L}_s$ размерностей $d_1,\ldots,d_s$, и в качестве базиса выбрано объединение базисов этих подпространств. Тогда в матрице преобразования могут отличаться от нуля только элементы квадратных клеток порядков $d_1,\ldots,d_s$ на диагонали:

$$A=\begin{Vmatrix}A_1& & &O\\ &A_2& & \\ & &\ddots& \\ O& & &A_s\end{Vmatrix}. \tag{3}$$

Такие матрицы называются клеточно-диагональными или блочно-диагональными. Итак, имеет место

---
**стр. 258**

---

П р е д л о ж е н и е 2. *Матрица линейного преобразования является клеточно-диагональной тогда и только тогда, когда базис есть объединение базисов инвариантных подпространств.*

Инвариантные подпространства преобразования $\mathsf{A}$ тесно связаны с преобразованиями, перестановочными с $\mathsf{A}$. Эту связь описывает

П р е д л о ж е н и е 3. *Если преобразования $\mathsf{A}$ и $\mathsf{B}$ перестановочны, то ядро и множество значений одного из них инвариантны относительно другого.*

Д о к а з а т е л ь с т в о. $1^\circ$. Если $x\in\operatorname{Ker}\mathsf{A}$, то $\mathsf{A}(x)=o$, и потому $\mathsf{B}(\mathsf{A}(x))=o$. Тогда $\mathsf{A}(\mathsf{B}(x))=o$, а значит $\mathsf{B}(x)\in\operatorname{Ker}\mathsf{A}$.

$2^\circ$. Если $x\in\operatorname{Im}\mathsf{A}$, то существует вектор $z$, такой, что $x=\mathsf{A}(z)$. Тогда $\mathsf{B}(x)=\mathsf{B}(\mathsf{A}(z))=\mathsf{A}(\mathsf{B}(z))$. Это означает, что $\mathsf{B}(x)\in\operatorname{Im}\mathsf{A}$.

**4. Собственные подпространства.** Мы найдем подпространство, инвариантное относительно заданного линейного преобразования $\mathsf{A}$, если найдем преобразование, перестановочное с $\mathsf{A}$ и имеющее ненулевое ядро. Перестановочны с $\mathsf{A}$, прежде всего, многочлены от $\mathsf{A}$ и, в частности, простейшие из них — линейные. С точностью до числового множителя линейному многочлену от $\mathsf{A}$ можно придать вид $\mathsf{A}-\lambda\mathsf{E}$, где $\lambda$ — некоторый коэффициент.

О п р е д е л е н и е. Если для числа $\lambda$ подпространство $\operatorname{Ker}(\mathsf{A}-\lambda\mathsf{E})$ — ненулевое, то $\lambda$ называется *собственным значением* преобразования, а подпространство — *собственным подпространством*, соответствующим (или принадлежащим) собственному значению $\lambda$.

Отметим один важный частный случай. Если преобразование $\mathsf{A}$ имеет ненулевое ядро, то это ядро — собственное подпространство, соответствующее собственному значению $\lambda=0$. Ограничение $\mathsf{A}$ на этом инвариантном подпространстве — нулевое преобразование.

Если вектор $x$ лежит в собственном подпространстве, то для него $(\mathsf{A}-\lambda\mathsf{E})(x)=o$ или $\mathsf{A}(x)-\lambda\mathsf{E}(x)=\mathsf{A}(x)-\lambda x=o$ и окончательно

$$\mathsf{A}(x)=\lambda x. \tag{4}$$

---
**стр. 259**

---

О п р е д е л е н и е. Вектор $x$ называется *собственным вектором* преобразования $\mathsf{A}$, соответствующим (или принадлежащим) собственному значению $\lambda$, если 1) $x\neq o$, 2) $\mathsf{A}(x)=\lambda x$.

Определение означает, что собственные векторы — это ненулевые векторы собственных подпространств.

Из равенства (4) следует

П р е д л о ж е н и е 4. *Ограничение преобразования на собственном подпространстве является или нулевым преобразованием, или гомотетией: оно умножает каждый вектор этого подпространства на собственное значение.*

Пусть нам каким-то образом удалось найти собственные значения преобразования $\mathsf{A}$. Тогда для нахождения собственных подпространств нужно для каждого собственного значения $\lambda$ составить систему линейных уравнений

$$(A-\lambda E)\mathbf{x}=\mathbf{o}, \tag{5}$$

где $A$ — матрица преобразования в некотором базисе $\mathbf{e}$. Фундаментальная система решений системы (5) состоит из координатных столбцов векторов, составляющих базис собственного подпространства. В развернутом виде система (5) записывается так:

$$\begin{aligned}
(\alpha^1_1-\lambda)\xi^1+\alpha^1_2\xi^2+\cdots+\alpha^1_n\xi^n&=0,\\
\alpha^2_1\xi^1+(\alpha^2_2-\lambda)\xi^2+\cdots+\alpha^2_n\xi^n&=0,\\
\ldots\ldots\ldots\ldots\ldots&\\
\alpha^n_1\xi^1+\alpha^n_2\xi^2+\cdots+(\alpha^n_n-\lambda)\xi^n&=0.
\end{aligned} \tag{6}$$

П р е д л о ж е н и е 5. *Собственные векторы и только они являются базисными векторами одномерных подпространств, инвариантных относительно $\mathsf{A}$.*

Д о к а з а т е л ь с т в о. $1^\circ$. Пусть вектор $x$ — собственный, а $y$ принадлежит одномерному подпространству $\mathcal{L}'$ с базисом $x$. Тогда $y=\alpha x$ и $\mathsf{A}(y)=\alpha\mathsf{A}(x)=\alpha\lambda x$. Значит $\mathsf{A}(y)$ лежит в $\mathcal{L}'$.

$2^\circ$. Пусть $x$ — базис инвариантного подпространства $\mathcal{L}'$. Тогда $\mathsf{A}(x)$ лежит в $\mathcal{L}'$ и раскладывается по базису: $\mathsf{A}(x)=\lambda x$. Так как $x\neq o$, он собственный.

---
**стр. 260**

---

С л е д с т в и е. *В собственном подпространстве через каждый вектор проходит одномерное инвариантное подпространство.*

П р е д л о ж е н и е 6. *В $i$-м столбце матрицы линейного преобразования все элементы вне главной диагонали равны нулю тогда и только тогда, когда $i$-й базисный вектор — собственный. В этом случае диагональный элемент столбца — собственное значение.*

Действительно, если базисный вектор $e_i$ — собственный, то $\mathsf{A}(e_i)=\lambda e_i$, и поэтому $i$-й элемент координатного столбца вектора $\mathsf{A}(e_i)$ равен $\lambda$, а остальные элементы равны нулю. Остается вспомнить, что координатный столбец $\mathsf{A}(e_i)$ есть $i$-й столбец матрицы преобразования. Обратное утверждение доказывается аналогично.

Из предложения 6 вытекает

П р е д л о ж е н и е 7. *Матрица преобразования $\mathsf{A}$ в базисе $e_1,\ldots,e_n$ диагональная тогда и только тогда, когда все базисные векторы — собственные. В этом случае диагональные элементы матрицы — соответствующие собственные значения.*

**5. Характеристическое уравнение.** Выберем базис и обозначим через $A$ матрицу линейного преобразования $\mathsf{A}$ в этом базисе. Тогда преобразование $\mathsf{A}-\lambda\mathsf{E}$ имеет матрицу $A-\lambda E$, и его ядро отлично от нуля тогда и только тогда, когда система (6) имеет ненулевое решение:

$$\det(A-\lambda E)=\det\begin{Vmatrix}\alpha_{1}^1-\lambda&\alpha_{2}^1&\ldots&\alpha_{n}^1\\ \alpha_{1}^2&\alpha_{2}^2-\lambda&\ldots&\alpha_{n}^2\\ \ldots&\ldots&\ldots&\ldots\\ \alpha_{1}^n&\alpha_{2}^n&\ldots&\alpha_{n}^n-\lambda\end{Vmatrix}=0. \tag{7}$$

Равенство (7), рассматриваемое как условие на $\lambda$, называется *характеристическим уравнением* матрицы $A$, а его корни — *характеристическими числами* матрицы $A$.

Разумеется, в вещественном пространстве в качестве множителей допускаются только вещественные числа, и собственные значения должны быть вещественными. В соответствии с этим имеет место

---
**стр. 261**

---

Т е о р е м а 1. *В комплексном пространстве все корни характеристического уравнения и только они являются собственными значениями. В вещественном пространстве то же справедливо для вещественных корней характеристического уравнения.*

Левая часть характеристического уравнения представляет собой многочлен степени $n$. Действительно, согласно формуле полного разложения ((14) § 4 гл. V) детерминант равен алгебраической сумме произведений, в каждое из которых входит по $n$ элементов матрицы. Содержат $\lambda$ только элементы, стоящие на главной диагонали. Существует одно произведение

$$(\alpha^1_1-\lambda)(\alpha^2_2-\lambda)\ldots(\alpha^n_n-\lambda), \tag{8}$$

в котором все сомножители содержат $\lambda$. Если в какое-нибудь другое произведение вошел сомножитель $\alpha^i_j$ ($i\neq j$), то в него не могут войти сомножители $(\alpha^i_i-\lambda)$ и $(\alpha^j_j-\lambda)$. Поэтому каждый член суммы, кроме (8) содержит $\lambda$ в степени не выше, чем $n-2$. Раскрывая скобки в выражении (8), выпишем два члена со старшими степенями $\lambda$:

$$(-1)^n\lambda^n+(-1)^{n-1}(\alpha^1_1+\alpha^2_2+\cdots+\alpha^n_n)\lambda^{n-1}.$$

Эти же члены будут старшими во всем многочлене. Свободный член многочлена равен его значению при $\lambda=0$, а это значение равно $\det(A-0E)=\det A$. Таким образом,

$$\det(A-\lambda E)=(-1)^n\lambda^n+(-1)^{n-1}\lambda^{n-1}\sum_{i=1}^n\alpha^i_i+\cdots+\det A. \tag{9}$$

Этот многочлен называется *характеристическим многочленом* матрицы $A$. Остальные его коэффициенты находить не будем, так как они нам не потребуются. Многочлен степени $n$, как известно, не может иметь больше, чем $n$ различных корней и всегда имеет хотя бы один комплексный корень. Если мы рассматриваем вещественное пространство, то может случиться (при четной размерности), что характеристическое уравнение не имеет ни одного вещественного корня, и, следовательно, линейное преобразование не имеет собственных

---
**стр. 262**

---

значений и собственных подпространств. Примером может служить поворот плоскости.

В комплексном пространстве и в вещественном пространстве нечетной размерности каждое линейное преобразование имеет хоть одно собственное значение и хоть одно собственное подпространство.

П р е д л о ж е н и е 8. *Если $A$ и $A'$ — матрицы линейного преобразования $\mathsf{A}$ в разных базисах, то характеристические многочлены этих матриц совпадают.*

Д о к а з а т е л ь с т в о. Согласно формуле (1) мы имеем $\det(A'-\lambda E)=\det(S^{-1}AS-\lambda S^{-1}S)=\det(S^{-1}(A-\lambda E)S)$. Заменим детерминант произведения произведением детерминантов сомножителей: $\det(A'-\lambda E)\det S^{-1}\det S=\det(A-\lambda E)$. Это то, что требовалось.

Из этого предложения следует, что мы можем назвать характеристический многочлен матрицы $A$ характеристическим многочленом преобразования $\mathsf{A}$.

Коэффициенты характеристического многочлена являются инвариантами, связанными с преобразованием. В частности, детерминант матрицы преобразования не зависит от выбора базиса. Другим важным инвариантом является коэффициент при $(-\lambda)^{n-1}$, называемый *следом* матрицы: $\alpha^1_1+\cdots+\alpha^n_n$. Он обозначается $\operatorname{tr}A$.

С помощью теоремы Виета из (9) нетрудно установить, что след матрицы равен сумме всех корней ее характеристического многочлена, а детерминант — произведению корней.

Напомним, что многочлен $p(t)$ делится на многочлен $q(t)$, если для всех $t$ выполнено равенство $p(t)=q(t)r(t)$, где $r(t)$ — некоторый многочлен. В этом случае все корни $q$ являются корнями $p$.

П р е д л о ж е н и е 9. *Если подпространство $\mathcal{L}'$ инвариантно относительно преобразования $\mathsf{A}$, то характеристический многочлен преобразования $\mathsf{A}$ делится на характеристический многочлен его ограничения на $\mathcal{L}'$.*

Действительно, согласно предложению 1 в базисе, первые векторы которого составляют базис инвариантного подпространства, матрица $A$ преобразования клеточно-треугольная,

---
**стр. 263**

---

то есть имеет вид (2), причем верхняя левая клетка $A_1$ этой матрицы — матрица ограничения $\mathsf{A}$ на $\mathcal{L}'$. Матрица $A-\lambda E$ также клеточно-треугольная, а по формуле (12) § 4 гл. V

$$\det(A-\lambda E)=\det(A_1-\lambda E)\det(A_4-\lambda E).$$

**6. Свойства собственных подпространств.** Взаимное расположение собственных подпространств описывает

Т е о р е м а 2. *Сумма собственных подпространств является прямой суммой.*

В силу предложения 5 § 2 это равносильно утверждению:

Собственные векторы $x_1,\ldots,x_s$, принадлежащие попарно различным собственным значениям $\lambda_1,\ldots,\lambda_s$, линейно независимы.

Для доказательства рассмотрим преобразования $\mathsf{B}_i=(\mathsf{A}-\lambda_i\mathsf{E})$ для всех $i=1,\ldots,s$ и образы векторов $x_1,\ldots,x_s$ при этих преобразованиях. Для любых $i$ и $j$ имеем

$$\mathsf{B}_i(x_j)=\mathsf{A}(x_j)-\lambda_i x_j=(\lambda_j-\lambda_i)x_j. \tag{10}$$

Таким образом, $\mathsf{B}_i(x_j)=o$ при $i\neq j$, а $\mathsf{B}_i(x_i)\neq o$. Допустим, что один из векторов раскладывается по остальным, например,

$$x_1=\alpha_2x_2+\cdots+\alpha_sx_s.$$

Подействуем на обе части равенства преобразованиями $\mathsf{B}_2,\ldots,\mathsf{B}_s$. Вектор $x_1$ в левой части равенства перейдет в отличный от нуля вектор $(\lambda_1-\lambda_2)\ldots(\lambda_1-\lambda_s)x_1$, а произвольное слагаемое $\alpha_jx_j$ ($j=2,\ldots,s$) в правой части равенства перейдет в

$$\alpha_j(\lambda_j-\lambda_2)\ldots(\lambda_j-\lambda_j)\ldots(\lambda_j-\lambda_s)x_j,$$

то есть в нулевой вектор. Поэтому вся правая часть равенства перейдет в нулевой вектор. Полученное противоречие заканчивает доказательство теоремы.

Напомним, что кратностью корня $\lambda_0$ многочлена $p(\lambda)$ называется самое большое число $s$, при котором многочлен $p(\lambda)$ делится на многочлен $(\lambda_0-\lambda)^s$. Корни кратности 1 называются простыми.

Т е о р е м а 3. *Если собственное значение $\lambda_0$ преобразования $\mathsf{A}$ есть корень характеристического многочлена кратности $s$, то размерность соответствующего собственного подпространства не превосходит $s$.*

---
**стр. 264**

---

Д о к а з а т е л ь с т в о. Пусть корню $\lambda_0$ соответствует собственное подпространство $\mathcal{L}'$ размерности $k$. Выберем там базис $e_1,\ldots,e_k$. Все векторы этого базиса — собственные с одним и тем же собственным значением $\lambda_0$. Поэтому согласно предложению 7 ограничение $\mathsf{A}$ на $\mathcal{L}'$ имеет в этом базисе диагональную матрицу, все диагональные элементы которой равны $\lambda_0$. Характеристический многочлен этой матрицы есть $q(\lambda)=(\lambda_0-\lambda)^k$. По предложению 9 в этом случае характеристический многочлен $p(\lambda)$ преобразования $\mathsf{A}$ делится на $(\lambda_0-\lambda)^k$. Следовательно, по определению кратности корня $k\leqslant s$. Теорема доказана.

Собственному значению кратности $s$ может принадлежать собственное подпространство размерности, меньшей, чем $s$. Например, читатель может проверить, что линейное преобразование двумерного пространства, задаваемое матрицей

$$\begin{Vmatrix}1&1\\0&1\end{Vmatrix},$$

имеет собственное значение кратности 2 и одномерное собственное подпространство.

**7. Комплексные характеристические числа.** Допустим, что у линейного преобразования $\mathsf{A}$ вещественного линейного пространства $\mathcal{L}$ характеристический многочлен имеет комплексный (не вещественный) корень $\lambda$. Поскольку коэффициенты многочлена вещественны, комплексно сопряженное число $\overline\lambda$ также будет корнем многочлена. Имеет место

П р е д л о ж е н и е 10. *Паре комплексно сопряженных корней характеристического многочлена преобразования $\mathsf{A}$ вещественного пространства соответствует ненулевое инвариантное подпространство $\mathcal{L}'$, обладающее тем свойством, что оно не содержит собственных векторов, а через любой его вектор проходит двумерное инвариантное подпространство.*

Д о к а з а т е л ь с т в о. Числа $\lambda$ и $\overline\lambda$ являются корнями вещественного квадратного трехчлена $t^2+pt+q$ (в котором $p=-(\lambda+\overline\lambda)$, а $q=\lambda\overline\lambda$). Рассмотрим линейное преобразование $\mathsf{B}=\mathsf{A}^2+p\mathsf{A}+q\mathsf{E}$ и подпространство $\mathcal{L}'=\operatorname{Ker}\mathsf{B}$. По предложению 3 $\mathcal{L}'$ инвариантно.

---
**стр. 265**

---

Докажем, что $\mathcal{L}'$ — ненулевое подпространство. Если в некотором базисе $\mathsf{A}$ имеет матрицу $A$, то матрицей преобразования $\mathsf{B}$ будет $A^2+pA+qE$. Эта матрица вещественна, но раскладывается на два комплексных множителя: $B=(A-\lambda E)(A-\overline\lambda E)$. Отсюда $\det B=\det(A-\lambda E)\det(A-\overline\lambda E)=0$, так как $\det(A-\lambda E)=0$, и мы видим, что ядро $\mathsf{B}$ ненулевое.

$\mathcal{L}'$ не содержит собственных векторов. Действительно, если для некоторого вектора $x$ выполнено $\mathsf{A}(x)=\mu x$, то $\mathsf{B}(x)=\mu^2x+p\mu x+qx=(\mu^2+p\mu+q)x$. Так как квадратный трехчлен не имеет вещественных корней, $\mu^2+p\mu+q\neq0$, и поэтому из $\mathsf{B}(x)=o$ следует $x=o$. Вектор $x$ не может быть собственным.

Пусть теперь $x$ — ненулевой вектор из $\mathcal{L}'$. Рассмотрим подпространство $\mathcal{L}''$ — линейную оболочку векторов $x$ и $\mathsf{A}(x)$. Это подпространство инвариантно. В самом деле, пусть $y=\alpha x+\beta\mathsf{A}(x)$ — вектор из $\mathcal{L}''$. Тогда $\mathsf{A}(y)=\alpha\mathsf{A}(x)+\beta\mathsf{A}^2(x)$. Так как $\mathsf{B}(x)=o$, мы находим, что $\mathsf{A}^2(x)=-p\mathsf{A}(x)-qx$, и потому $\mathsf{A}(y)=\alpha\mathsf{A}(x)-\beta p\mathsf{A}(x)-\beta qx$. Значит, $\mathsf{A}(y)$ раскладывается по $x$ и $\mathsf{A}(x)$, то есть принадлежит $\mathcal{L}''$.

Итак, линейная оболочка векторов $x$ и $\mathsf{A}(x)$ инвариантное подпространство. Ясно, что его размерность не больше двух. Если бы она равнялась 1, то подпространство содержало бы собственный вектор, чего, как мы видели, быть не может. Предложение доказано.

Рассмотрим корни характеристического многочлена. Если среди них найдется вещественный, то существует собственное подпространство, а значит и одномерное инвариантное подпространство. Если найдется не вещественный корень, то найдется двумерное инвариантное подпространство. Поэтому имеет место

С л е д с т в и е. *Любое линейное преобразование ненулевого вещественного пространства имеет или одномерное или двумерное инвариантное подпространство.*

**8. Приведение матрицы преобразования к диагональному виду.** Если все векторы базиса — собственные, то в нем матрица преобразования диагональная (предложение 7). Для произвольного линейного преобразования такого базиса может не существовать (пример на стр. 264). Если он

---
**стр. 266**

---

существует, то говорят, что матрица преобразования приводится к диагональному виду, а преобразование называют *диагонализуемым* или преобразованием *простой структуры*.

П р е д л о ж е н и е 11. *Преобразование $\mathsf{A}$ пространства $\mathcal{L}$ диагонализуемо тогда и только тогда, когда $\mathcal{L}$ совпадает с суммой собственных подпространств $\mathsf{A}$.*

Д о к а з а т е л ь с т в о. Если $\mathcal{L}$ совпадает с суммой собственных подпространств, то в $\mathcal{L}$ есть базис из собственных векторов, так как сумма собственных подпространств — прямая, и объединение их базисов — базис в $\mathcal{L}$. Обратно, если есть базис из собственных векторов, то каждый вектор раскладывается по собственным векторам и потому принадлежит сумме собственных подпространств.

Следующее предложение дает простое, но важное достаточное условие диагонализуемости преобразования.

П р е д л о ж е н и е 12. *Если преобразование $n$-мерного пространства $\mathcal{L}$ имеет $n$ попарно различных собственных значений, то оно диагонализуемо.*

Действительно, соответствующие этим $n$ собственным значениям собственные подпространства расположены так, что их сумма — прямая. Самое меньшее, каждое из них имеет размерность 1, и потому размерность суммы не может быть меньше, чем $n$. Значит сумма собственных подпространств должна совпадать с $\mathcal{L}$.

Условие в предложении 12 не является необходимым. Вполне может случиться, что линейное преобразование имеет меньше, чем $n$, собственных значений, но все же имеет базис из собственных векторов. В самом деле, мы можем произвольно задать базис и взять преобразование, имеющее в этом базисе диагональную матрицу с несколькими равными элементами на диагонали. Если все элементы диагонали одинаковы (в частности, для тождественного и нулевого преобразований), то каждый ненулевой вектор будет собственным, и в каждом базисе матрица преобразования будет диагональной.

Предложению 12 можно придать следующую форму.

П р е д л о ж е н и е 13. *Если все характеристические числа матрицы $A$ попарно различны, то существует невы-*

---
**стр. 267**

---

*рожденная матрица $S$ такая, что матрица $S^{-1}AS$ — диагональная. Если матрица $A$ вещественна, а ее характеристические числа попарно различны и вещественны, то существует такая вещественная матрица $S$.*

Т е о р е м а 4. *Линейное преобразование $\mathsf{A}$ пространства $\mathcal{L}$ диагонализуемо тогда и только тогда, когда оно удовлетворяет уравнению $r(\mathsf{A})=O$, где $r(t)$ — некоторый многочлен без кратных (а для вещественного пространства и комплексных) корней. При этом все собственные значения преобразования — корни многочлена.*

Д о к а з а т е л ь с т в о. $1^\circ$. Пусть $\mathsf{A}$ диагонализуемо. Это значит, что $\mathcal{L}$ раскладывается в сумму собственных подпространств, и каждый вектор $x$ представим в виде суммы $x=x_1+\cdots+x_s$ собственных векторов, принадлежащих попарно различным собственным значениям $\lambda_1,\ldots,\lambda_s$. Рассмотрим многочлен $r(t)=(t-\lambda_1)\ldots(t-\lambda_s)$, и покажем, что преобразование $\mathsf{R}=r(\mathsf{A})=(\mathsf{A}-\lambda_1\mathsf{E})\ldots(\mathsf{A}-\lambda_s\mathsf{E})$ — нулевое. Действительно, согласно (10) $(\mathsf{A}-\lambda_i\mathsf{E})(x_j)=(\lambda_j-\lambda_i)x_j$, и потому

$$\mathsf{R}(x_j)=(\lambda_j-\lambda_1)\ldots(\lambda_j-\lambda_j)\ldots(\lambda_j-\lambda_s)x_j=o.$$

Отсюда вытекает, что $\mathsf{R}(x)=\mathsf{R}(x_1)+\cdots+\mathsf{R}(x_s)=o$ для любого $x$.

$2^\circ$. Обратно, пусть $\mathsf{A}$ удовлетворяет уравнению $r(\mathsf{A})=O$, в котором $r(t)=(t-\alpha_1)\ldots(t-\alpha_s)$. Разложим функцию $1/r(t)$ на элементарные дроби:

$$\frac{1}{r(t)}=\frac{\beta_1}{t-\alpha_1}+\cdots+\frac{\beta_s}{t-\alpha_s}.$$

После приведения к общему знаменателю получим тождество

$$1=q_1(t)+\cdots+q_s(t),$$

где $q_i(t)=\beta_ir(t)/(t-\alpha_i)$ — произведение $\beta_i$ на многочлен, получаемый из $r(t)$ вычеркиванием множителя $t-\alpha_i$. Подставим в это тождество преобразование $\mathsf{A}$ вместо переменной $t$: $\mathsf{E}=q_1(\mathsf{A})+\cdots+q_s(\mathsf{A})$. Подействуем обеими частями полученного равенства на произвольный вектор $x$. Мы получим

---
**стр. 268**

---

$x=x_1+\cdots+x_s$, где $x_i=q_i(\mathsf{A})(x)$.

Если $x_i\neq o$, то $x_i$ — собственный вектор, принадлежащий собственному значению $\alpha_i$. Действительно, $(\mathsf{A}-\alpha_i\mathsf{E})(x_i)=\beta_ir(\mathsf{A})(x)=o$. Таким образом, произвольный вектор пространства разложен в сумму собственных векторов. Это равносильно тому, что $\mathcal{L}$ раскладывается в сумму собственных подпространств.

**9. Приведение матрицы преобразования к треугольному виду.** Выясним, при каких условиях для линейного преобразования $\mathsf{A}$ пространства $\mathcal{L}$ можно выбрать такой базис, в котором его матрица будет верхней треугольной.

Предварительно докажем следующее

П р е д л о ж е н и е 14. *Если преобразование $\mathsf{A}$ $n$-мерного пространства $\mathcal{L}$ имеет собственное значение $\lambda$, то в $\mathcal{L}$ найдется инвариантное подпространство $\mathcal{L}'$ размерности $n-1$, содержащее $\operatorname{Im}(\mathsf{A}-\lambda\mathsf{E})$.*

Д о к а з а т е л ь с т в о. Пусть $\lambda$ — собственное значение. Тогда $\dim\operatorname{Ker}(\mathsf{A}-\lambda\mathsf{E})\geqslant1$, а следовательно, $\dim\operatorname{Im}(\mathsf{A}-\lambda\mathsf{E})\leqslant n-1$. Если здесь имеет место равенство, то в силу предложения 3 утверждение доказано. В противном случае возьмем какое-нибудь $(n-1)$-мерное подпространство $\mathcal{L}'$, содержащее $\operatorname{Im}(\mathsf{A}-\lambda\mathsf{E})$.

Рассмотрим произвольный вектор $x$ из $\mathcal{L}'$. Для него $\mathsf{A}(x)=(\mathsf{A}(x)-\lambda x)+\lambda x$. Здесь $(\mathsf{A}(x)-\lambda x)\in\operatorname{Im}(\mathsf{A}-\lambda\mathsf{E})\subset\mathcal{L}'$ и $\lambda x\in\mathcal{L}'$. Таким образом, $\mathcal{L}'$ инвариантно, и предложение доказано.

Т е о р е м а 5. *В комплексном линейном пространстве для каждого линейного преобразования существует базис, в котором матрица преобразования — верхняя треугольная.*

*В вещественном пространстве то же утверждение справедливо, если все корни характеристического многочлена преобразования вещественны.*

Заметим, что диагональные элементы треугольной матрицы — корни ее характеристического многочлена. Поэтому условие во второй части теоремы необходимо.

Д о к а з а т е л ь с т в о. Рассмотрим сначала $n$-мерное комплексное пространство $\mathcal{L}_n$ и его линейное преобразование $\mathsf{A}$. В комплексном пространстве любой корень характеристического многочлена является собственным значением, и следо-

---
**стр. 269**

---

вательно, $\mathsf{A}$ имеет собственное значение $\lambda_n$. В силу предложения 14 найдется $(n-1)$-мерное инвариантное подпространство $\mathcal{L}_{n-1}$.

Рассмотрим ограничение $\mathsf{A}^{(n-1)}$ преобразования $\mathsf{A}$ на $\mathcal{L}_{n-1}$. У него существует собственное значение $\lambda_{n-1}$ и, следовательно, $(n-2)$-мерное инвариантное подпространство $\mathcal{L}_{n-2}$, которое, разумеется, инвариантно относительно $\mathsf{A}$. Далее, последовательно рассматривая ограничения $\mathsf{A}^{(n-i)}$ на инвариантных подпространствах $\mathcal{L}_{n-i}$ для всех $i=2,\ldots,n-2$ мы получим цепочку вложенных инвариантных относительно $\mathsf{A}$ подпространств

$$\mathcal{L}_1\subset\ldots\subset\mathcal{L}_{n-2}\subset\mathcal{L}_{n-1}. \tag{11}$$

Здесь подпространство $\mathcal{L}_k$ имеет размерность $k$, $k=1,\ldots,n-1$. При этом для любого $i=n,n-1,\ldots,2$

$$(\mathsf{A}-\lambda_i\mathsf{E})(\mathcal{L}_i)\subseteq\mathcal{L}_{i-1}. \tag{12}$$

Теперь выберем базис, связанный с построенными подпространствами. Базисный вектор $e_1$ поместим в $\mathcal{L}_1$. Дополним этот вектор до базиса $e_1,e_2$ в подпространстве $\mathcal{L}_2$, этот базис дополним до базиса $e_1,e_2,e_3$ в подпространстве $\mathcal{L}_3$. Продолжая далее таким же образом, мы получим базис $e_1,\ldots,e_n$ в $\mathcal{L}_n$. (Последний вектор дополняет базис в $\mathcal{L}_{n-1}$ до базиса в $\mathcal{L}_n$.)

При каждом $k$ вектор $e_k$ лежит в инвариантном подпространстве $\mathcal{L}_k$, а значит, его образ раскладывается по векторам с номерами, не большими, чем $k$. Таким образом, матрица преобразования $\mathsf{A}$ в построенном базисе — верхняя треугольная.

В комплексном пространстве на каждом этапе существование собственного значения сомнений не вызывает. В вещественном пространстве мы предполагаем, что все корни характеристического многочлена вещественны. Согласно предложению 9 для каждого инвариантного подпространства все корни характеристического многочлена ограничения являются корнями характеристического многочлена $\mathsf{A}$, а потому также вещественны. Таким образом, и в вещественном пространстве

---
**стр. 270**

---

при наших предположениях на каждом этапе построения базиса существование собственного значения гарантировано.

Приведем матричную формулировку теоремы 5.

*Для любой квадратной матрицы $A$ найдется невырожденная матрица $S$ такая, что матрица $S^{-1}AS$ — верхняя треугольная с корнями характеристического многочлена $A$ на диагонали.*

*Если матрица $A$ вещественная с вещественными корнями характеристического многочлена, то и $S$ может быть выбрана вещественной.*

**10. Теорема Гамильтона–Кэли.** Так называется следующая

Т е о р е м а 6. *Пусть $p(\lambda)$ — характеристический многочлен линейного преобразования $\mathsf{A}$. Тогда $p(\mathsf{A})=O$.*

Д о к а з а т е л ь с т в о. Пусть сначала $\mathcal{L}$ — комплексное пространство и $\mathsf{A}$ — его линейное преобразование, имеющее корни характеристического уравнения $\lambda_1,\ldots,\lambda_n$ (возможно, равные).

Рассмотрим линейное преобразование $\mathsf{P}=p(\mathsf{A})$. Нам нужно доказать, что

$$\mathsf{P}=(-1)^n(\mathsf{A}-\lambda_1\mathsf{E})\cdot\ldots\cdot(\mathsf{A}-\lambda_n\mathsf{E})=O. \tag{13}$$

Это будет доказано, если мы покажем, что $\operatorname{Im}\mathsf{P}=\mathsf{P}(\mathcal{L})=\{o\}$. Вспомним доказательство теоремы 5 и будем считать, что нумерация собственных значений совпадает с введенной там нумерацией. Действие преобразования $\mathsf{P}$ сводится к последовательному выполнению преобразований $(\mathsf{A}-\lambda_i\mathsf{E})$ для $i=n,(n-1),\ldots,2,1$. Согласно (12) $(\mathsf{A}-\lambda_n\mathsf{E})(\mathcal{L})\subseteq\mathcal{L}_{n-1}$. Далее, последовательно применяем включение (12) для всех $i=n-1,\ldots,2$, и в результате видим, что произведение

$$(\mathsf{A}-\lambda_2\mathsf{E})\cdot\ldots\cdot(\mathsf{A}-\lambda_n\mathsf{E})$$

отображает пространство $\mathcal{L}$ в подпространство $\mathcal{L}_1$. Это подпространство одномерное и инвариантное, а следовательно, собственное, и $(\mathsf{A}-\lambda_1\mathsf{E})(\mathcal{L}_1)=\{o\}$. Итак, для комплексного пространства теорема доказана. Но ее легко распространить и на вещественные пространства.

---
**стр. 271**

---

Именно, произвольную комплексную матрицу $A$ можно рассматривать как матрицу линейного преобразования $\mathsf{A}$ комплексного пространства, и если $p(t)$ — характеристический многочлен этой матрицы, то $p(\mathsf{A})=O$ и, следовательно, $p(A)=O$. Вещественная матрица может рассматриваться как комплексная, поэтому и каждая вещественная матрица удовлетворяет своему характеристическому уравнению, а значит теорема верна и для преобразований вещественного пространства. Это заканчивает доказательство.

Одновременно мы получили матричную формулировку теоремы Гамильтона–Кэли:

П р е д л о ж е н и е 15. *Каждая квадратная матрица удовлетворяет своему характеристическому уравнению.*

### Упражнения

1. Докажите, что каждое подпространство, лежащее в $\operatorname{Ker}\mathsf{A}$, и каждое подпространство, содержащее $\operatorname{Im}\mathsf{A}$, инвариантны относительно $\mathsf{A}$.

2. Докажите, что сумма и пересечение инвариантных подпространств инвариантны.

3. Докажите, что размерность подпространства $\mathcal{L}'$, определенного в предложении 10 — четное число.

4. Пусть $\mathsf{A}:\mathcal{L}\to\mathcal{L}$. Докажите, что $\mathcal{L}=\operatorname{Ker}\mathsf{A}\oplus\operatorname{Im}\mathsf{A}$ тогда и только тогда, когда $\operatorname{Ker}\mathsf{A}^2=\operatorname{Ker}\mathsf{A}$.

5. Пусть $\mathcal{L}=\operatorname{Ker}\mathsf{A}\oplus\operatorname{Im}\mathsf{A}$. Какой вид имеет матрица преобразования $\mathsf{A}$ в базисе $\mathbf{e}$, если $e_1,\ldots,e_r\in\operatorname{Im}\mathsf{A}$, а $e_{r+1},\ldots,e_n\in\operatorname{Ker}\mathsf{A}$?

---
**стр. 272**

---

6. В некотором базисе преобразование задано матрицей
$$\begin{Vmatrix}0&1\\1&0\end{Vmatrix}.$$
Существует ли базис, в котором его матрица диагональная? Если да, то найдите этот базис.

7. Найдите собственные значения и собственные подпространства преобразования, заданного матрицей
$$\begin{Vmatrix}3&-2&6\\-2&6&3\\6&3&-2\end{Vmatrix}.$$

8. Каждой квадратной матрице порядка $n$ сопоставляется ее транспонированная матрица. Этим определено преобразование $\mathsf{T}$ пространства матриц. Найдите его собственные значения и собственные подпространства. Докажите из этих соображений, что каждая матрица однозначно представляется как сумма симметричной и кососимметричной.

9. Пусть $\mathsf{A}$ — поворот трехмерного векторного пространства вокруг некоторой оси на угол $\alpha$, и $A$ — матрица этого поворота в некотором базисе. Выразите $\alpha$ через элементы $A$.

10. Пусть $\mathsf{A}$ диагонализуемо. Докажите, что ограничение $\mathsf{A}$ на любом инвариантном подпространстве также диагонализуемо.

11. Преобразование $\mathsf{A}$ задано матрицей
$$\begin{Vmatrix}-2&4&-1\\-5&8&-2\\-9&13&-3\end{Vmatrix}.$$
Найдите матрицу перехода к какому-нибудь базису, в котором его матрица верхняя треугольная, а также матрицу $A$ в этом базисе.

12. Пусть матрица $A$ невырождена. Представьте матрицу $A^{-1}$ как многочлен от $A$.

13. Пусть преобразование $\mathsf{A}$ удовлетворяет соотношению $\mathsf{A}^2=\mathsf{A}$. Докажите, что $\operatorname{Rg}A=\operatorname{tr}A$.

Решения упражнений 1–13: [[../reshebnik_beklemishev/rbek_g06_s04|Решебник, Гл. VI §4]] (решебник решает также доп. упражнение 14*, отсутствующее в списке учебника).
