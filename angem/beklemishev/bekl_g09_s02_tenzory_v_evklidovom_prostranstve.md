# Глава IX. Основы тензорной алгебры

## § 2. Тензоры в евклидовом пространстве

**1. Метрический тензор.** Все, сказанное о тензорах в линейном пространстве, разумеется, справедливо и в случае евклидова пространства. Однако в евклидовом пространстве тензоры обладают многими свойствами, которых они не имеют в линейном.

О п р е д е л е н и е. Сопоставим каждому базису евклидова пространства матрицу Грама этого базиса. Определяемый этим тензор $g_{ij}$ типа $(0,2)$ называется *метрическим тензором* пространства.

Как мы видели в примере 6 § 1, справедливо

П р е д л о ж е н и е 1. *Сопоставим каждому базису евклидова пространства матрицу, обратную матрице Грама этого базиса. Это соответствие определяет тензор $g^{ij}$ типа $(2,0)$.*

О п р е д е л е н и е. Тензор, построенный в предложении 1, называется *контравариантным метрическим тензором*.

Поскольку $(\Gamma^{-1})^T=(\Gamma^T)^{-1}=\Gamma^{-1}$, контравариантный метрический тензор симметричен:

$$g^{ij} = g^{ji}.$$

Напишем равенство $\Gamma\Gamma^{-1}=E$ в тензорных обозначениях:

$$g_{ij}g^{jk} = \delta^k_i.$$

**2. Поднятие и опускание индексов.** Наличие метрического тензора позволяет ввести в евклидовом пространстве еще две операции над тензорами — поднятие и опускание индексов.

При *опускании индекса* тензору типа $(p,q)$, $p\geqslant 1$, сопоставляется тензор типа $(p-1,q+1)$, получаемый свертыванием данного тензора с метрическим тензором по тому индексу, который мы хотим опустить. При этом порядок индексов сохраняется в следующем смысле. Мы отказываемся от соглашения, согласно которому нижние индексы следуют за верхними. Для того, чтобы отметить порядок индексов, над

---
**стр. 396**

---

каждым нижним индексом и под каждым верхним индексом ставится точка. Например, при опускании первого индекса у тензора $\alpha^{ij}_{\cdot k}$ мы получаем тензор $g_{il}\alpha^{lj}_{\cdot k}=\alpha^{j\cdot}_{i\cdot k}$.

В действительности эти точки расставляются не всегда, а тогда, когда возможны недоразумения или неоднозначность в интерпретации формул. Часто можно обойтись без них.

При *поднятии индекса* данный тензор свертывается с контравариантным метрическим тензором по тому индексу, который следует поднять. Результат будет тензором типа $(p+1,q-1)$. Например, поднятие первого индекса у $\alpha^i_{\cdot jk}$ дает $\alpha^{ij\cdot}_{\cdot k}=g^{il}\alpha_{l\cdot jk}$, а поднятие третьего приводит к $\alpha^{\cdot jk}_{i\cdot}=g^{kl}\alpha^{\cdot j}_{i\cdot l}$.

> *Примечание к транскрипции:* точечная нотация для отслеживания исходного порядка индексов при поднятии/опускании (введена автором на этой странице) в оригинале печатается двумя строго выровненными по столбцам строками индексов; в инлайн-LaTeX такое выравнивание передать невозможно, поэтому здесь точки расставлены по непосредственному прочтению скана и могут не идеально совпадать с оригинальной постолбцовой версткой — при возникновении сомнений сверяться со страницами 396–397 источника.

П р и м е р 1. В § 3 гл. VII был введен вектор, присоединенный линейной функции на евклидовом пространстве. Строка коэффициентов $\boldsymbol{\varphi}$ линейной функции и координатный столбец вектора $\mathbf{a}$ связаны формулой $\boldsymbol{\varphi}=\mathbf{a}^T\Gamma$. Переходя от матричной записи к тензорной, мы получаем $\varphi_i=\alpha^kg_{ki}$. Итак, линейная функция получается из вектора опусканием индекса. Наоборот, вектор получается из функции поднятием индекса:

$$\alpha^j = g^{ij}\varphi_i.$$

П р и м е р 2. К билинейной функции $\mathsf{b}$ на евклидовом пространстве присоединено линейное преобразование $\mathbf{A}$, матрица которого связана с матрицей билинейной функции равенством $A=\Gamma^{-1}B$. В тензорных обозначениях это может быть переписано как

$$\alpha^i_j = g^{ik}\beta_{kj}.$$

Мы видим, что тензор $\mathbf{A}$ получен из тензора $\mathsf{b}$ поднятием первого индекса. Мы можем сказать также, что $\mathsf{b}$ получается из $\mathbf{A}$ опусканием индекса, но здесь уже необходимо подчеркнуть, что при опускании верхний индекс становится первым нижним индексом:

$$\beta_{ij} = g_{ik}\alpha^k_{\cdot j}.$$

То, что здесь это существенно, показывает

П р и м е р 3. В § 2 гл. VII мы определили линейное преобразование $\mathbf{A}^*$, сопряженное данному преобразованию $\mathbf{A}$. В произвольном базисе их матрицы связаны равенством (3), которое можно переписать в виде $\Gamma A^* = (\Gamma A)^T$. Обозначим

---
**стр. 397**

---

через $\alpha^i_j$ и $\alpha^{*i}_j$ элементы $A$ и $A^*$ и напишем это равенство в тензорных обозначениях:

$$g_{ik}\alpha^{*k}_{\cdot j} = g_{jk}\alpha^k_{\cdot i}$$

— после опускания индексов один тензор получается из другого транспонированием.

Свертывая обе части этого равенства по индексу $j$ с тензором $g^{lj}$, мы получим $\delta^l_k\alpha^{*k}_{\cdot i} = g^{lj}g_{ik}\alpha^k_{\cdot j}$ или

$$\alpha^{*l}_{\cdot i} = \alpha^l_{\cdot i}.$$

**3. Евклидовы тензоры.** При изучении евклидова пространства часто можно ограничиться только ортонормированными базисами. При этом все формулы, связанные со скалярным произведением, значительно упрощаются, так как метрический тензор имеет единичную матрицу:

$$g_{ij} = \begin{cases} 1, & i=j, \\ 0, & i\neq j. \end{cases} \tag{1}$$

Матрица перехода от одного ортонормированного базиса к другому ортонормированному является ортогональной, то есть удовлетворяет соотношению $S^{-1}=S^T$, а ее элементы связаны равенствами

$$\sigma^i_k = \tau^i_k. \tag{2}$$

Пусть мы ограничились ортонормированными базисами. Тогда в силу (2) закон преобразования компонент тензора примет вид

$$\alpha'^{\,i_1\ldots i_p}_{j_1\ldots j_q} = \sum_{k_1,\ldots,k_p}\sigma^{k_1}_{i_1}\ldots\sigma^{k_p}_{i_p}\sigma^{l_1}_{j_1}\ldots\sigma^{l_q}_{j_q}\alpha^{k_1\ldots k_p}_{l_1\ldots l_q}. \tag{3}$$

Здесь нарушились правила тензорной символики: индексы $i_1,\ldots,i_p$ в левой части равенства верхние, а в правой — нижние. Кроме того, пришлось написать знак $\sum$, так как индексы суммирования $k_1,\ldots,k_p$ оказались все сверху. Это — признаки того, что равенство не является инвариантным (оно верно только в ортонормированных базисах).

---
**стр. 398**

---

Формула (3) показывает, что, ограничиваясь ортонормированными базисами, мы уничтожаем различие между верхними и нижними индексами: и тем и другим в законе преобразования соответствуют одинаковые множители.

Заметим еще, что в силу (1) в ортонормированном базисе совпадают компоненты тензоров, отличающихся друг от друга на поднятие или опускание индекса. Мы отмечали это в гл. VII для векторов, присоединенных линейным функциям, и для преобразований, присоединенных билинейным функциям. Это легко проверяется и в любом случае. Мы имеем, например, $\alpha_{ijk}=g_{jl}\alpha^l_{i\cdot k}=\alpha^{j\cdot}_{i\cdot k}$, так как в сумме по $l$ отличное от нуля только слагаемое с $l=j$, а в нем $g_{jl}=1$.

Из сказанного следует, что, ограничиваясь ортонормированными базисами, мы можем отождествить все тензоры, которые отличаются друг от друга поднятием или опусканием индекса. Точнее говоря, все тензоры, имеющие в ортонормированных базисах одинаковые компоненты, мы объединяем в один класс и рассматриваем этот класс как некоторый новый объект — *евклидов тензор*.

О п р е д е л е н и е. В евклидовом пространстве размерности $n$ задан *евклидов тензор валентности $s$*, если каждому ортонормированному базису сопоставлена $s$-мерная матрица порядка $n$. При этом каковы бы ни были ортонормированные базисы $\mathbf{e}$ и $\mathbf{e}'$, соответствующие им $\alpha_{i_1\ldots i_s}$ и $\alpha'_{k_1\ldots k_s}$ связаны соотношениями

$$\alpha'_{k_1\ldots k_s} = \sigma^{i_1}_{k_1}\ldots\sigma^{i_s}_{k_s}\alpha_{i_1\ldots i_s}. \tag{4}$$

Все индексы у евклидовых тензоров равноправны, и мы пишем их внизу. По повторяющимся индексам, как всегда, производится суммирование.

Числовые величины, не меняющиеся при переходе от одного ортонормированного базиса к другому в гл. VIII, были названы *ортогональными* (или *евклидовыми*) *инвариантами*. Теперь мы видим, что это — евклидовы тензоры валентности 0.

Компоненты евклидова тензора в неортонормированных базисах не определены. Однако для каждого евклидова тензора

---
**стр. 399**

---

валентности $s$ можно определить эти компоненты так, чтобы получился тензор любого типа $(p,q)$, где $p+q=s$. Для этого их нужно найти, исходя из компонент евклидова тензора в ортонормированном базисе, при помощи закона преобразования компонент тензора типа $(p,q)$. Таким образом, каждый евклидов тензор порождается любым тензором из некоторого класса тензоров. Ясно, что все тензоры этого класса отличаются друг от друга поднятием или опусканием индексов.

Рассмотрим вектор евклидова пространства и присоединенную к нему линейную функцию. В ортонормированном базисе их компоненты совпадают, а в неортонормированном — различаются. В этом случае компоненты линейной функции называются *ковариантными координатами* евклидова вектора, определяемого рассматриваемым вектором. Это — координаты вектора в биортогональном базисе.

Важным примером евклидова тензора является так называемый *дискриминантный тензор*, определяемый для некоторого ортонормированного базиса равенством

$$\varepsilon_{i_1\ldots i_n} = (-1)^{N(i_1\ldots i_n)}$$

(где $N$ — число нарушений порядка в соответствующей перестановке, если все индексы различны, и $\varepsilon_{i_1\ldots i_n}=0$ в противном случае. Как мы видели в задаче 3 § 1, компоненты такого тензора преобразуются по формуле $\varepsilon'_{i_1\ldots i_n}=\varepsilon_{i_1\ldots i_n}\det S$, что в случае ортогональной матрицы перехода дает $\varepsilon'_{i_1\ldots i_n}=\pm\varepsilon_{i_1\ldots i_n}$ — компоненты дискриминантного тензора одинаковы во всех ортонормированных базисах одной ориентации с исходным и отличаются только знаком в базисах противоположной ориентации. Для неортонормированных базисов дискриминантный тензор доопределяется как тензор типа $(0,n)$.

Для евклидовых тензоров определены все тензорные операции, определенные для тензоров в § 1. Определения, а также формулировки и доказательства свойств этих операций были бы почти дословным повторением сказанного, и мы не приводим их. Заметим только, что для евклидовых тензоров свертывание возможно по любой паре индексов, и транспонировать, симметрировать и альтернировать можно по любому

---
**стр. 400**

---

множеству индексов. Например, если, ограничиваясь ортонормированными базисами, мы отождествим квадратичную форму с присоединенным к ней линейным преобразованием, то полученный новый объект — евклидов тензор валентности 2 — будет иметь инвариантную свертку (как линейное преобразование) и инвариантно будет удовлетворять условию симметрии $\alpha_{ij}=\alpha_{ji}$ (как квадратичная форма). Инвариантность здесь, конечно, только относительно замены одного ортонормированного базиса другим ортонормированным.

### Упражнения

1. В базисе $\mathbf{e}$ метрический тензор задан матрицей $\Gamma$, а тензор $a_{ij}$ матрицей $A$:

$$\Gamma = \left\|\begin{matrix}1&1\\1&2\end{matrix}\right\|, \quad A = \left\|\begin{matrix}3&4\\5&7\end{matrix}\right\|.$$

Найдите матрицы $B$, $C$ и $D$ тензоров $a^{k\cdot}_{\cdot j}$, $a^{\cdot k}_{j\cdot}$ и $a^{jk}$.

2. Можно заметить, что в задаче 1 детерминанты матриц $A$, $B$, $C$ и $D$ совпадают, и следы $B$ и $C$ одинаковы. Объясните это.

3. Упростите выражение

$$\left(\delta_r^ma_{ij}^rg^{jk}+\delta_i^ja_{lj}^mg^{lk}\right)g_{ks}+a_{[jk]}^mg^{jk}g_{is}.$$

Решения: [[../reshebnik_beklemishev/rbek_g09_s02|Решебник, §2]].
