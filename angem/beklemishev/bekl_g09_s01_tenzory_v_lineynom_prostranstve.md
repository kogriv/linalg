# Глава IX. Основы тензорной алгебры

## § 1. Тензоры в линейном пространстве

**1. Вводные замечания.** В предыдущих главах мы рассматривали в линейных или евклидовых пространствах различного рода объекты: линейные преобразования, билинейные функции и т. д. Изучение каждого объекта основывалось на определении, которое формулировалось без участия базиса. Например, линейное преобразование определялось как такое отображение пространства в то же пространство, которое удовлетворяет определенным условиям ((1) § 3 гл. VI). Таким образом, изученные нами объекты существуют и в принципе могут быть изучены без введения базиса. Для обозначения таких объектов мы будем пользоваться термином *геометрический объект*.

Хотя геометрический объект и существует независимо от базиса, бывает удобно, выбрав некоторый базис, задать объект относительно этого базиса при помощи упорядоченной системы чисел — компонент объекта. Например, выбор базиса устанавливает взаимно однозначное соответствие между линейными преобразованиями и квадратными матрицами. Элементы матрицы линейного преобразования можно считать компонентами линейного преобразования в рассматриваемом базисе. Неизменность объекта при замене базиса приводит к изменению компонент.

Во всех встречавшихся случаях мы могли вычислить компоненты объекта в одном базисе через его компоненты в другом базисе и через элементы матрицы перехода от первого

---
**стр. 376**

---

базиса ко второму. Такая зависимость называется *законом преобразования компонент геометрического объекта*.

В этой главе мы рассмотрим важный класс геометрических объектов, называемых *тензорами*. Закон преобразования их компонент таков, что новые компоненты являются линейными однородными многочленами от старых компонент, а коэффициенты этих многочленов являются произведениями элементов матрицы перехода и элементов обратной к ней матрицы. Для того, чтобы точно описать этот закон и тем самым дать определение тензора, необходимо ввести некоторые обозначения.

**2. Обозначения.** Напомним, что матрицу размеров $m\times n$ можно определить как функцию, сопоставляющую некоторое число каждой паре $(i,j)$, где $i\in\{1,\ldots,m\}$, а $j\in\{1,\ldots,n\}$. Обобщим это определение. Поскольку нам будут нужны только матрицы, аналогичные квадратным матрицам, значения всех индексов будут принадлежать одному и тому же множеству $\{1,\ldots,n\}$.

О п р е д е л е н и е. *$s$-мерной матрицей порядка $n$* (или *$s$-мерным массивом*) называется функция, определенная на множестве всевозможных наборов чисел $(i_1,\ldots,i_s)$, где числа принадлежат множеству $\{1,\ldots,n\}$.

Для того, чтобы разъяснить термин «$s$-мерная матрица», рассмотрим трехмерную матрицу с элементами $\alpha_{ijk}$. При любом фиксированном значении индекса $k=k_0$ элементы вида $\alpha_{ijk_0}$ составляют квадратную матрицу порядка $n$. Таким образом, вся совокупность элементов трехмерной матрицы распадается в упорядоченный набор из $n$ квадратных матриц: $\|\alpha_{ij1}\|,\ldots,\|\alpha_{ijn}\|$. Можно представить себе эти матрицы расположенными одна под другой в виде слоев, так что образуется куб, разделенный на $n^3$ ячеек, содержащих по одному числу. Аналогично, четырехмерная матрица может рассматриваться как упорядоченный набор трехмерных матриц и так далее.

Строку и столбец удобно считать одномерными матрицами: их элементы нумеруются одним индексом.

В рассматриваемых нами матрицах не все индексы будут равноправны: будут выделены два сорта индексов. При-

---
**стр. 377**

---

нято индексы одного сорта писать вверху, а другого — внизу. В принципе мы могли бы, скажем, обозначать один сорт индексов латинскими буквами, а другой — греческими, но принятое обозначение жестко связано со всей системой обозначений. За расположением индексов приходится строго следить. Если порядок индексов не установлен иначе, мы будем считать, что нижние индексы следуют за верхними так, как если бы они были написаны правее верхних.

Многомерные матрицы полностью выписывать сложно. Действует следующее соглашение: буквенный индекс рассматривают как переменную величину, принимающую значения $1,\ldots,n$, и если написано выражение, содержащее буквенный индекс[^1], не являющийся индексом суммирования, то предполагается, что написаны $n$ таких выражений, соответствующих каждому значению индекса. Когда имеется несколько индексов, сказанное относится к каждому из них. Таким образом, например, $\alpha^{i_1\ldots i_s}$ обозначает всю совокупность элементов $s$-мерной матрицы, запись $\alpha^i_{jk}=\beta^i_{jk}$ означает, что стоящие на одинаковых местах элементы двух трехмерных матриц равны, то есть матрицы равны, а равенство $\alpha_{ik}=\alpha_{ki}$ означает, что матрица $\alpha_{ik}$ симметрична.

Вводится следующее новое обозначение суммирования. Пусть написан одночлен, состоящий из букв с индексами, причем какой-то индекс встречается дважды: один раз вверху, а другой раз внизу. Это обозначает сумму членов такого вида, написанных для всех значений повторяющегося индекса, так, как если бы перед ним стоял знак $\Sigma$, а индекс был индексом суммирования, принимающим значения от 1 до $n$.

Если описанным образом повторяются несколько индексов, то имеется в виду многократная сумма. Раньше мы постоянно сталкивались с подобными суммами, но теперь мы этого делать не будем. Например,

[^1]: У нас в качестве буквенных индексов, как правило, будут применяться буквы $i$, $j$, $k$, $l$. Буква $n$ всегда обозначает фиксированное число — размерность пространства.

---
**стр. 378**

---

формулы

$$\mathsf{f}(x) = \sum_{i=1}^n \varphi_i \xi^i, \quad \beta'_{kl} = \sum_{i,j} \beta_{ij}\sigma^i_k\sigma^j_l$$

будем писать в виде

$$\mathsf{f}(x) = \varphi_i \xi^i, \quad \beta'_{kl} = \beta_{ij}\sigma^i_k\sigma^j_l.$$

**3. Определение и примеры.** Мы рассматриваем $n$-мерное вещественное линейное пространство $\mathcal{L}$.

О п р е д е л е н и е. В пространстве $\mathcal{L}$ задан *тензор типа $(p,q)$*, если каждому базису сопоставлена $p+q$-мерная матрица порядка $n$. При этом, каковы бы ни были базисы $\mathbf{e}$ и $\mathbf{e}'$, соответствующие им матрицы $\alpha^{i_1\ldots i_p}_{j_1\ldots j_q}$ и $\alpha'^{\,i_1\ldots i_p}_{j_1\ldots j_q}$ должны быть связаны следующими соотношениями:

$$\alpha'^{\,i_1\ldots i_p}_{j_1\ldots j_q} = \tau^{i_1}_{k_1}\ldots\tau^{i_p}_{k_p}\,\sigma^{l_1}_{j_1}\ldots\sigma^{l_q}_{j_q}\,\alpha^{k_1\ldots k_p}_{l_1\ldots l_q}, \tag{1}$$

где $\sigma^i_j$ — элементы матрицы перехода от $\mathbf{e}$ к $\mathbf{e}'$, а $\tau^i_k$ — элементы ее обратной матрицы.

В правой части (1) суммирование осуществляется по всем индексам $k$ и $l$, то есть мы имеем $p+q$-кратную сумму. Принято писать сверху те индексы, которым в каждом из $n^{p+q}$ слагаемых соответствуют множители $\tau$. Эти индексы называются *контравариантными*. Внизу пишутся те индексы, которым соответствуют множители $\sigma$. Это — *ковариантные* индексы.

Элементы матрицы, соответствующей некоторому базису, называются *компонентами тензора* в этом базисе. Число $p+q$ называется *валентностью* тензора, а $q$ и $p$ соответственно ко- и *контравариантной валентностью*.

Подчеркнем, что несмотря на сложность суммы в правой части формулы (1), в каждое слагаемое входит одна единственная компонента тензора. Это означает, что новые компоненты являются линейными однородными многочленами относительно старых компонент. Сложность формулы (1) связана с выражением коэффициентов этих многочленов через элементы матрицы перехода.

---
**стр. 379**

---

Два тензора равны, если они одного типа и имеют одинаковые компоненты в некотором базисе. Тогда из закона преобразования вытекает, что равны их компоненты в любом базисе.

Для любой $p+q$-мерной матрицы и любого базиса $\mathbf{e}_0$ найдется тензор типа $(p,q)$, который в базисе $\mathbf{e}_0$ имеет эту матрицу компонент. Его компоненты в остальных базисах могут быть найдены с помощью формулы (1). Здесь еще следует убедиться, что так полученные матрицы компонент в любых базисах $\mathbf{e}_1$ и $\mathbf{e}_2$ будут связаны равенством вида (1). Не будем этим заниматься, так как существование тензора типа $(p,q)$ с произвольными компонентами в выбранном базисе будет видно из примера 9.

П р и м е р 1. *Вектор является тензором типа $(1,0)$.* Действительно, если задан вектор, то каждому базису соответствует одномерная матрица — столбец. При этом, компоненты, соответствующие разным базисам, связаны формулой $\mathbf{x}=S\mathbf{x}'$ или $\mathbf{x}'=S^{-1}\mathbf{x}$:

$$\xi'^i = \tau^i_k\xi^k.$$

Это — закон преобразования компонент тензора типа $(1,0)$.

П р и м е р 2. *Линейная функция на пространстве $\mathcal{L}$ является тензором типа $(0,1)$.* Действительно, если задана линейная функция, то каждому базису соответствует одномерная матрица — строка коэффициентов этой линейной функции. При изменении базиса коэффициенты линейной функции преобразуются по формуле $\boldsymbol{\varphi}'=\boldsymbol{\varphi}S$, то есть

$$\varphi'_i = \sigma^k_i\varphi_k.$$

Тензоры типа $(0,1)$ — векторы сопряженного пространства $\mathcal{L}^*$ — называют *ковекторами*.

П р и м е р 3. *Линейное преобразование пространства $\mathcal{L}$ является тензором типа $(1,1)$.* В самом деле, если задано линейное преобразование, то каждому базису соответствует матрица, и матрицы, соответствующие двум базисам, связаны формулой $A'=S^{-1}AS$:

$$\alpha'^{\,i}_j = \tau^i_k\sigma^l_j\alpha^k_l.$$

---
**стр. 380**

---

П р и м е р 4. Важным тензором типа $(1,1)$ является так называемый *символ Кронекера*, компоненты которого в некотором базисе составляют единичную матрицу:

$$\delta^i_j = \begin{cases} 0, & i\neq j, \\ 1, & i=j. \end{cases} \tag{2}$$

Формула (2) — принятое обозначение, и мы будем им ниже пользоваться. Если интерпретировать символ Кронекера как линейное преобразование, то это будет тождественное преобразование $E$, и потому в любом другом базисе этот тензор имеет те же компоненты, составляющие единичную матрицу. Для примера проверим это, используя тензорную символику. Согласно закону преобразования,

$$\delta'^{\,i}_j = \tau^i_k\sigma^l_j\delta^k_l. \tag{3}$$

Если $\delta^k_l$ определяется формулой (2), то из $n^2$ слагаемых в правой части (3) равны нулю все, кроме тех, для которых $k=l$. Поэтому $\delta'^{\,i}_j = \tau^i_k\sigma^k_j$, а $\tau^i_k\sigma^k_j$ — элементы произведения $S^{-1}S$. Значит, $\delta'^{\,i}_j=\delta^i_j$.

П р и м е р 5. *Билинейная функция на пространстве $\mathcal{L}$ — тензор типа $(0,2)$.* Если дана такая функция, то каждому базису сопоставляется ее матрица, и матрицы билинейной функции в разных базисах связаны формулой $B'=S^TBS$:

$$\beta'_{ij} = \sigma^k_i\sigma^l_j\beta_{kl}.$$

Важно отметить, что симметричная билинейная функция и соответствующая ей квадратичная форма — один и тот же тензор, поскольку их матрицы в любом базисе совпадают.

П р и м е р 6. Пусть $B$ — матрица билинейной функции ранга $n$ в базисе $\mathbf{e}$. Сопоставим этому базису матрицу $C=B^{-1}$. Сделав это для всех базисов, мы получим тензор типа $(2,0)$. Действительно, из $B'=S^TBS$ следует $B'^{-1}=S^{-1}B^{-1}(S^T)^{-1}=S^{-1}B^{-1}(S^{-1})^T$ или $C'=S^{-1}C(S^{-1})^T$. Если $\gamma^{ij}$ — элементы матрицы $C$, то это можно записать как

$$\gamma'^{\,ij} = \tau^i_k\tau^j_l\gamma^{kl}.$$

---
**стр. 381**

---

П р и м е р 7. Число, не зависящее от выбора базиса — *инвариант* — можно считать тензором типа $(0,0)$.

П р и м е р 8. Рассмотрим обобщение билинейной функции — функцию $\mathsf{F}(x_1,\ldots,x_q)$ от $q$ векторов, линейную по каждому из них, если остальные фиксированы. Такие функции называются *$q$-линейными* или *полилинейными*, если число аргументов не уточняется. Разложим каждый из векторов по некоторому базису $\mathbf{e}$. Тогда в силу полилинейности

$$\mathsf{F}(x_1,\ldots,x_q) = \mathsf{F}(\xi_1^{i_1}e_{i_1},\ldots,\xi_q^{i_q}e_{i_q}) = \xi_1^{i_1}\ldots\xi_q^{i_q}\mathsf{F}(e_{i_1},\ldots,e_{i_q}) = \xi_1^{i_1}\ldots\xi_q^{i_q}\alpha_{i_1\ldots i_q},$$

где коэффициенты $\alpha_{i_1\ldots i_q}=\mathsf{F}(e_{i_1},\ldots,e_{i_q})$ играют ту же роль, что и элементы матрицы билинейной функции. Докажем, что при замене базиса они преобразуются как компоненты тензора типа $(0,q)$. Для этого рассмотрим базис $e'_i = \sigma^k_ie_k$ и снова воспользуемся полилинейностью:

$$\mathsf{F}(e'_{i_1},\ldots,e'_{i_q}) = \mathsf{F}(\sigma^{k_1}_{i_1}e_{k_1},\ldots,\sigma^{k_q}_{i_q}e_{k_q}) = \sigma^{k_1}_{i_1}\ldots\sigma^{k_q}_{i_q}\mathsf{F}(e_{k_1},\ldots,e_{k_q}),$$

или $\alpha'_{i_1\ldots i_q} = \sigma^{k_1}_{i_1}\ldots\sigma^{k_q}_{i_q}\alpha_{k_1\ldots k_q}$, как и требовалось.

П р и м е р 9. Таким же способом можно построить пример тензора любого типа $(p,q)$. При этом полилинейная функция должна зависеть от $q$ векторов и $p$ ковекторов. Значение такой функции на векторах $x_1,\ldots,x_q$ и ковекторах $\mathsf{f}^1,\ldots,\mathsf{f}^p$ можно вычислить, разложив векторы по базису $\mathbf{e}$, а ковекторы — по его биортогональному базису $\mathbf{e}^*$ в пространстве $\mathcal{L}^*$. Напомним, что базис $\mathrm{e}^{*1},\ldots,\mathrm{e}^{*n}$ называется биортогональным базису $e_1,\ldots,e_n$, если

$$\mathrm{e}^{*i}(e_k) = \delta^i_k.$$

Если $x_i = \xi_i^{k_i}e_{k_i}$, а $\mathsf{f}^j = \varphi^j_{l_j}\mathrm{e}^{*l_j}$, то аналогично предыдущему получим

$$\mathsf{F}(x_1,\ldots,x_q,\mathsf{f}^1,\ldots,\mathsf{f}^p) = \xi_1^{k_1}\ldots\xi_q^{k_q}\varphi^1_{l_1}\ldots\varphi^p_{l_p}\alpha^{l_1\ldots l_p}_{k_1\ldots k_q},$$

---
**стр. 382**

---

где

$$\alpha^{l_1\ldots l_p}_{k_1\ldots k_q} = \mathsf{F}(e_{k_1},\ldots,e_{k_q},\mathrm{e}^{*l_1},\ldots,\mathrm{e}^{*l_p}).$$

Вспомним, что базис $\mathbf{e}^*$ преобразуется матрицей $(S^{-1})^T$. В тензорных обозначениях это записывается как $\mathrm{e}'^{*j} = \tau^j_k\mathrm{e}^{*k}$ и проверяется так:

$$\mathrm{e}'^{*j}(e'_i) = \tau^j_k\mathrm{e}^{*k}(\sigma^l_ie_l) = \tau^j_k\sigma^l_i\delta^k_l = \delta^j_i.$$

Теперь подставим в $\alpha'^{\,i_1\ldots i_p}_{j_1\ldots j_q} = \mathsf{F}(e'_{j_1},\ldots,e'_{j_q},\mathrm{e}'^{*i_1},\ldots,\mathrm{e}'^{*i_p})$ выражения новых базисных векторов через старые (для обоих базисов $\mathbf{e}$ и $\mathbf{e}^*$) и, как и в примере 8, получим закон преобразования коэффициентов, который будет совпадать с законом преобразования (1).

Этот пример показывает, что для любой $p+q$-мерной матрицы и любого базиса $\mathbf{e}$ найдется тензор типа $(p,q)$ с такой матрицей компонент в базисе $\mathbf{e}$.

**4. Линейные операции.** Линейные операции определены для тензоров одного и того же типа. Именно, определим для пространственных матриц одинаковой размерности сложение и умножение на число поэлементно: суммой матриц $\alpha^{i_1\ldots i_p}_{j_1\ldots j_q}$ и $\beta^{i_1\ldots i_p}_{j_1\ldots j_q}$ назовем матрицу

$$\gamma^{i_1\ldots i_p}_{j_1\ldots j_q} = \alpha^{i_1\ldots i_p}_{j_1\ldots j_q} + \beta^{i_1\ldots i_p}_{j_1\ldots j_q}, \tag{4}$$

а произведением матрицы $\alpha^{i_1\ldots i_p}_{j_1\ldots j_q}$ на число $\lambda$ — матрицу

$$\beta^{i_1\ldots i_p}_{j_1\ldots j_q} = \lambda\alpha^{i_1\ldots i_p}_{j_1\ldots j_q}.$$

П р е д л о ж е н и е 1. *Пусть $\mathbf{A}$ и $\mathbf{B}$ — тензоры типа $(p,q)$. Сопоставим каждому базису сумму их матриц в этом базисе. Этим будет определен тензор типа $(p,q)$. Сопоставим каждому базису произведение матрицы тензора $\mathbf{A}$ на число $\lambda$. Этим будет определен тензор того же типа $(p,q)$.*

Обе части предложения доказываются одинаково и, по существу, вытекают из того, что правая часть формулы (1) — линейный однородный многочлен относительно старых компонент тензора. Приведем доказательство для первой части. При

---
**стр. 383**

---

замене базиса

$$\alpha'^{\,i_1\ldots i_p}_{j_1\ldots j_q} = \tau^{i_1}_{k_1}\ldots\tau^{i_p}_{k_p}\sigma^{l_1}_{j_1}\ldots\sigma^{l_q}_{j_q}\alpha^{k_1\ldots k_p}_{l_1\ldots l_q}$$

и $\beta'^{\,i_1\ldots i_p}_{j_1\ldots j_q} = \tau^{i_1}_{k_1}\ldots\tau^{i_p}_{k_p}\sigma^{l_1}_{j_1}\ldots\sigma^{l_q}_{j_q}\beta^{k_1\ldots k_p}_{l_1\ldots l_q}$.

Складывая почленно эти равенства, мы получаем

$$\alpha'^{\,i_1\ldots i_p}_{j_1\ldots j_q} + \beta'^{\,i_1\ldots i_p}_{j_1\ldots j_q} = \tau^{i_1}_{k_1}\ldots\tau^{i_p}_{k_p}\sigma^{l_1}_{j_1}\ldots\sigma^{l_q}_{j_q}(\alpha^{k_1\ldots k_p}_{l_1\ldots l_q} + \beta^{k_1\ldots k_p}_{l_1\ldots l_q}),$$

то есть тензорный закон преобразования для $\alpha^{k_1\ldots k_p}_{l_1\ldots l_q}+\beta^{k_1\ldots k_p}_{l_1\ldots l_q}$.

Тензоры, определенные в предложении 1, мы назовем, соответственно, *суммой* тензоров $\mathbf{A}$ и $\mathbf{B}$ и *произведением тензора $\mathbf{A}$ на число $\lambda$*. Свойства линейных операций описываются следующим предложением.

П р е д л о ж е н и е 2. *По отношению к операциям сложения и умножения на число множество всех тензоров одного и того же типа $(p,q)$ является линейным пространством размерности $n^{p+q}$.*

Предоставим читателю проверить все аксиомы в определении линейного пространства и займемся размерностью. Выберем в $\mathcal{L}$ какой-нибудь базис и рассмотрим тензоры, у которых из компонент в данном базисе равна 1, а остальные компоненты равны нулю. Существует ровно $n^{p+q}$ таких тензоров, так как тензор типа $(p,q)$ имеет $n^{p+q}$ компонент. Каждый тензор данного типа раскладывается, и притом однозначно, по выбранным нами тензорам (коэффициенты разложения равны компонентам данного тензора). Таким образом, размерность пространства тензоров типа $(p,q)$ равна $n^{p+q}$, и предложение доказано.

Более того, мы построили базис в пространстве тензоров типа $(p,q)$, естественным образом связанный с базисом в пространстве $\mathcal{L}$. Напомним, что как раз таким способом мы построили базис в сопряженном пространстве $\mathcal{L}^*$ — пространстве тензоров типа $(0,1)$ — и назвали его биортогональным исходному базису в $\mathcal{L}$. Теперь для пространства $\mathcal{L}$ мы имеем бесконечную последовательность линейных пространств, связанных с ним так же, как $\mathcal{L}^*$: как только выбран базис в $\mathcal{L}$, во всех этих пространствах также появляются базисы.

---
**стр. 384**

---

**5. Умножение тензоров.** Пусть $\mathbf{A}$ — тензор типа $(p,q)$, а $\mathbf{B}$ — тензор типа $(r,s)$. Произвольному базису $\mathbf{e}$ мы можем сопоставить $(p+r)+(q+s)$-мерную матрицу, составленную из произведений каждой компоненты $\mathbf{A}$ на каждую компоненту $\mathbf{B}$. Эти произведения упорядочим, записав сначала индексы, относящиеся к $\mathbf{A}$, а затем индексы, относящиеся к $\mathbf{B}$, так, как показывает формула

$$\gamma^{i_1\ldots i_p\,k_1\ldots k_r}_{j_1\ldots j_q\,l_1\ldots l_s} = \alpha^{i_1\ldots i_p}_{j_1\ldots j_q}\beta^{k_1\ldots k_r}_{l_1\ldots l_s}. \tag{5}$$

П р е д л о ж е н и е 3. *Если каждому базису мы сопоставим числа $\gamma^{i_1\ldots i_p\,k_1\ldots k_r}_{j_1\ldots j_q\,l_1\ldots l_s}$, определяемые формулой (5), то этим будет определен тензор типа $(p+r,\,q+s)$.*

Доказательство мы проведем для случая тензоров типов $(1,1)$ и $(0,1)$. В общем случае доказательство отличается только более громоздкой записью. Выразим компоненты тензоров $\mathbf{A}$ и $\mathbf{B}$ в базисе $\mathbf{e}'$ через их компоненты в базисе $\mathbf{e}$:

$$\alpha'^{\,i}_j = \tau^i_k\sigma^l_j\alpha^k_l, \quad \beta'_m = \sigma^h_m\beta_h.$$

Отсюда

$$\gamma'^{\,i}_{jm} = \alpha'^{\,i}_j\beta'_m = \tau^i_k\sigma^l_j\sigma^h_m\alpha^k_l\beta_h = \tau^i_k\sigma^l_j\sigma^h_m\gamma^k_{lh},$$

то есть величины $\gamma^k_{lh}$ преобразуются при замене базиса как компоненты тензора типа $(1,2)$.

О п р е д е л е н и е. Тензор, построенный в предложении 3, называется *произведением* тензора $\mathbf{A}$ на тензор $\mathbf{B}$ и обозначается $\mathbf{A}\otimes\mathbf{B}$.

П р и м е р 10. Рассмотрим две линейные функции $\mathsf{f}$ и $\mathsf{h}$ на $\mathcal{L}$ и сопоставим каждой паре векторов $x$ и $y$ число $\mathsf{b}(x,y)=\mathsf{f}(x)\mathsf{h}(y)$. Пусть в некотором базисе значения функций записываются как $\mathsf{f}(x)=\varphi_i\xi^i$ и $\mathsf{h}(y)=\mu_k\eta^k$, где $\xi^i$ и $\eta^k$ — компоненты векторов $x$ и $y$. Тогда

$$\mathsf{b}(x,y) = \mathsf{f}(x)\mathsf{h}(y) = (\varphi_i\xi^i)(\mu_k\eta^k) = (\varphi_i\mu_k)\xi^i\eta^k,$$

поскольку при перемножении многочленов каждый член одного сомножителя умножается на каждый член другого. Итак,

---
**стр. 385**

---

построенная нами функция $\mathsf{b}$ — произведение двух линейных функций — билинейная функция, то есть тензор типа $(0,2)$. Она является тензорным произведением тензоров, соответствующих $\mathsf{f}$ и $\mathsf{h}$. Мы можем написать $\mathsf{b}=\mathsf{f}\otimes\mathsf{h}$ или, в компонентах, $\beta_{ik}=\varphi_i\eta_k$.

Тензорное произведение не коммутативно. Это хорошо видно на предыдущем примере. Пусть $\mathsf{b}^*=\mathsf{h}\otimes\mathsf{f}$. Тогда $\mathsf{b}^*(x,y)=\mathsf{h}(x)\mathsf{f}(y)=\mathsf{b}(y,x)$, то есть это будет другая билинейная функция, если только функция $\mathsf{b}$ не симметрична. Посмотрим на то же самое с точки зрения компонент. Конечно, $\varphi_i\eta_k = \beta^*_{ki}$ — матрицы билинейных функций отличаются на транспонирование: $B^*=B^T$. Они совпадают, если $\varphi_i\eta_k-\eta_i\varphi_k=0$ для всех $i,k$. Равенство нулю всех этих детерминантов второго порядка равносильно пропорциональности коэффициентов линейных функций $\mathsf{f}$ и $\mathsf{h}$.

И в общем случае множество чисел, являющихся компонентами произведения тензоров, не зависит от порядка сомножителей, но упорядочиваются эти числа по-разному в зависимости от порядка сомножителей.

Предоставим читателю самостоятельно убедиться, что умножение тензоров ассоциативно и дистрибутивно по отношению к сложению. Легко заметить также, что произведение тензора на число совпадает с произведением на тензор $(0,0)$, имеющий это число в качестве компоненты.

П р е д л о ж е н и е 4. *Любой тензор типа $(p,q)$ раскладывается в линейную комбинацию произведений, в каждое из которых входит $p$ векторов и $q$ ковекторов.*

Для доказательства покажем, что произведениями требуемого вида являются тензоры, из которых в предложении 2 был построен базис в пространстве тензоров типа $(p,q)$. Мы сделаем это для тензоров типа $(2,1)$, поскольку в общем случае рассуждение аналогично.

Пусть тензор $\mathbf{Q}$ таков, что в базисе $\mathbf{e}$ его компонента $\theta^{23}_1=1$, а остальные компоненты равны нулю. Рассмотрим векторы базиса $e_2,e_3$ и ковектор $\mathrm{e}^{*1}$, входящий в биортогональный базис. Векторы $e_2$ и $e_3$ имеют компоненты $(0,1,0,\ldots,0)$ и $(0,0,1,0,\ldots,0)$, а компоненты ковектора — $(1,0,\ldots,0)$. Поэтому произведение $\mathbf{Q} = e_2\otimes e_3\otimes\mathrm{e}^{*1}$ имеет только одну

---
**стр. 386**

---

компоненту $\theta^{23}_1$, равную 1, а все остальные его компоненты равны нулю. Точно также утверждение доказывается и для остальных тензоров, составляющих базис в пространстве тензоров рассматриваемого типа.

**6. Свертывание.** Рассмотрим множество элементов $s$-мерной матрицы, для которых все индексы, кроме некоторых двух, имеют фиксированные значения. Это множество образует *двумерный слой* — квадратную матрицу. Таким образом, вся матрица распадается на двумерные слои, соответствующие выбранной паре индексов. Всего таких слоев столько, сколько комбинаций значений могут принимать остальные $s-2$ индекса, то есть $n^{s-2}$.

Пусть $\mathbf{A}$ тензор типа $(p,q)$, причем $p>0$ и $q>0$ — тензор имеет как верхние, так и нижние индексы. Выберем какой-нибудь верхний, например, первый индекс, и какой-нибудь нижний, например, последний, и рассмотрим слои, соответствующие этой паре индексов. Напомним, что следом квадратной матрицы называется сумма ее диагональных элементов. Следы всех слоев образуют $p+q-2$-мерную матрицу, имеющую $p-1$ верхний и $q-1$ нижний индекс:

$$\beta^{i_2\ldots i_p}_{j_1\ldots j_{q-1}} = \alpha^{1\,i_2\ldots i_p}_{j_1\ldots j_{q-1}\,1} + \alpha^{2\,i_2\ldots i_p}_{j_1\ldots j_{q-1}\,2} + \cdots + \alpha^{n\,i_2\ldots i_p}_{j_1\ldots j_{q-1}\,n}. \tag{6}$$

Используя тензорное обозначение суммирования, мы можем написать

$$\beta^{i_2\ldots i_p}_{j_1\ldots j_{q-1}} = \alpha^{k\,i_2\ldots i_p}_{j_1\ldots j_{q-1}\,k}.$$

П р е д л о ж е н и е 5. *Сопоставим каждому базису систему чисел, получаемую из компонент тензора типа $(p,q)$ вычислением следа каждого слоя, соответствующего одному верхнему и одному нижнему индексам. Такое соответствие определяет тензор типа $(p-1,q-1)$.*

Для доказательства выясним, как преобразуется указанная система чисел при изменении базиса. Для случая, описанного формулой (6), мы имеем

$$\beta'^{\,i_2\ldots i_p}_{j_1\ldots j_{q-1}} = \alpha'^{\,k\,i_2\ldots i_p}_{j_1\ldots j_{q-1}\,k} = \tau^k_{m_1}\tau^{i_2}_{m_2}\ldots\tau^{i_p}_{m_p}\sigma^{l_1}_{j_1}\ldots\sigma^{l_{q-1}}_{j_{q-1}}\sigma^{m_1}_{k}\ldots\alpha^{m_1\ldots m_p}_{l_1\ldots l_q}.$$

---
**стр. 387**

---

Но так как $\tau^k_{m_1}\sigma^{l_q}_k = \delta^{l_q}_{m_1}$, это выражение равно

$$\delta^{l_q}_{m_1}\tau^{i_2}_{m_2}\ldots\tau^{i_p}_{m_p}\sigma^{l_1}_{j_1}\ldots\sigma^{l_{q-1}}_{j_{q-1}}\alpha^{m_1\ldots m_p}_{l_1\ldots l_q}.$$

При суммировании по индексам $l_q$ и $m_1$ равны нулю все слагаемые, за исключением тех, для которых $l_q=m_1$. Обозначив $l_q=m_1=k$, мы можем написать

$$\beta'^{\,i_2\ldots i_p}_{j_1\ldots j_{q-1}} = \tau^{i_2}_{m_2}\ldots\tau^{i_p}_{m_p}\sigma^{l_1}_{j_1}\ldots\sigma^{l_{q-1}}_{j_{q-1}}\alpha^{k\,m_2\ldots m_p}_{l_1\ldots l_{q-1}\,k}.$$

Это и есть доказываемый закон преобразования.

О п р е д е л е н и е. Тензор, получаемый из тензора $\mathbf{A}$ по формулам (6), называется его *сверткой* по первому верхнему и последнему нижнему индексу. Аналогично определяется свертка по любому верхнему и любому нижнему индексу.

Подчеркнем, что для двух верхних (или двух нижних) индексов свертка не определена.

Свертка тензора типа $(1,1)$ по единственной паре индексов есть инвариант — уже упоминавшийся след линейного преобразования.

*Сверткой двух тензоров* называется свертка их произведения по верхнему индексу одного из сомножителей и нижнему индексу другого. Например, образ вектора $x$ с компонентами $\xi^i$ при линейном преобразовании с матрицей $\alpha^i_k$ есть свертка соответствующих тензоров: $\eta^k = \alpha^k_i\xi^i$. Значение линейной функции $\mathsf{f}$ со строкой коэффициентов $\varphi_k$ на векторе $x$ с координатами $\xi^i$ есть свертка $\mathsf{f}(x)=\varphi_k\xi^k$.

**7. Транспонирование.** *Транспонированием* $s$-мерной матрицы по каким-либо двум индексам называется такая перестановка ее элементов, при которой транспонируется каждый слой, получаемый фиксированием всех индексов, кроме двух выбранных. Например, при транспонировании матрицы $\alpha^{ijk}_l$ по двум первым верхним индексам она переходит в матрицу $\beta^{ijk}_l$, связанную с ней равенством

$$\beta^{ijk}_l = \alpha^{jik}_l. \tag{7}$$

Вообще, под *транспонированием* матрицы по множеству индексов понимается результат ее последовательных

---
**стр. 388**

---

транспонирований по различным парам индексов из этого множества. По множеству из $k$ индексов может быть осуществлено $k!$ транспонирований.

Транспонирование иногда называют *перестановкой индексов*, хотя, например, записи $\alpha^k_{ij}$ и $\alpha^k_{ji}$ определяют одну и ту же матрицу: в обоих случаях все индексы независимо друг от друга принимают значения от 1 до $n$.

П р и м е р 11. Пусть $n=2$. Рассмотрим трехмерную матрицу $\alpha_{ijk}$. Значениям 1 и 2 последнего индекса соответствуют два слоя. Выпишем их рядом:

$$\left\|\begin{matrix}\alpha_{111} & \alpha_{121} \\ \alpha_{211} & \alpha_{221}\end{matrix}\right\|\ \left\|\begin{matrix}\alpha_{112} & \alpha_{122} \\ \alpha_{212} & \alpha_{222}\end{matrix}\right\|.$$

Транспонирование по двум первым индексам переводит эту матрицу в $\beta_{ijk}=\alpha_{jik}$, или, в развернутом виде,

$$\left\|\begin{matrix}\beta_{111} & \beta_{121} \\ \beta_{211} & \beta_{221}\end{matrix}\right\|\ \left\|\begin{matrix}\beta_{112} & \beta_{122} \\ \beta_{212} & \beta_{222}\end{matrix}\right\| = \left\|\begin{matrix}\alpha_{111} & \alpha_{211} \\ \alpha_{121} & \alpha_{221}\end{matrix}\right\|\ \left\|\begin{matrix}\alpha_{112} & \alpha_{212} \\ \alpha_{122} & \alpha_{222}\end{matrix}\right\|.$$

Если при более сложном транспонировании $\gamma_{ijk}=\alpha_{kij}$, то

$$\left\|\begin{matrix}\gamma_{111} & \gamma_{121} \\ \gamma_{211} & \gamma_{221}\end{matrix}\right\|\ \left\|\begin{matrix}\gamma_{112} & \gamma_{122} \\ \gamma_{212} & \gamma_{222}\end{matrix}\right\| = \left\|\begin{matrix}\alpha_{111} & \alpha_{112} \\ \alpha_{121} & \alpha_{122}\end{matrix}\right\|\ \left\|\begin{matrix}\alpha_{211} & \alpha_{212} \\ \alpha_{221} & \alpha_{222}\end{matrix}\right\|.$$

П р е д л о ж е н и е 6. *Пусть каждому базису сопоставлена $p+q$-мерная матрица, полученная из матрицы тензора $\mathbf{A}$ типа $(p,q)$ транспонированием, причем переставляются только верхние (или только нижние) индексы. Этим определен тензор $\mathbf{B}$ типа $(p,q)$.*

Нам достаточно доказать это для транспонирований по двум индексам, так как любое транспонирование — результат последовательного выполнения таких транспонирований. Кроме того, для любой пары верхних или нижних индексов доказательство одинаково. Число и расположение индексов, не участвующих в транспонировании, роли не играет. Поэтому

---
**стр. 389**

---

мы проведем доказательство для транспонирования по первой паре верхних индексов, записанного в формуле (7). Транспонируем матрицу новых компонент тензора $\mathbf{A}$:

$$\beta'^{\,ijk}_l = \alpha'^{\,jik}_l = \tau^j_{j_1}\tau^i_{i_1}\tau^k_{k_1}\sigma^{l_1}_l\alpha^{i_1j_1k_1}_{l_1} = \tau^j_{j_1}\tau^i_{i_1}\tau^k_{k_1}\sigma^{l_1}_l\beta^{i_1j_1k_1}_{l_1}.$$

Это отличается от обычной записи закона преобразования компонент тензора типа $(3,1)$ только порядком сомножителей.

О п р е д е л е н и е. Тензор $\mathbf{B}$, построенный в предложении 6, называется результатом *транспонирования* тензора $\mathbf{A}$.

Для двух тензоров $\mathbf{P}$ и $\mathbf{Q}$ произведения $\mathbf{P}\otimes\mathbf{Q}$ и $\mathbf{Q}\otimes\mathbf{P}$ получаются один из другого транспонированием.

**8. Симметрирование и альтернирование.** Рассмотрим тензор $\mathbf{A}$, контравариантная валентность которого не меньше заданного числа $s\geqslant 2$. Выберем какие-нибудь $s$ верхних индексов. Эти индексы можно переставить $s!$ способами, и потому существуют $s!$ тензоров, получающихся из $\mathbf{A}$ транспонированием по этим индексам. Сложим все эти тензоры и разделим результат на число $s!$. Полученный тензор называется результатом *симметрирования* $\mathbf{A}$ по выбранным индексам. Его компоненты обозначаются заключением в круглые скобки этой группы индексов у компонент тензора $\mathbf{A}$. Аналогично определяется симметрирование по нижним индексам.

П р и м е р 12. Симметрирование тензора типа $(3,0)$ по первому и третьему индексам:

$$\alpha^{(i|j|k)} = \tfrac{1}{2}(\alpha^{ijk}+\alpha^{kji}).$$

Обратите внимание, что второй индекс, не участвующий в симметрировании, выделен прямыми чертами.

П р и м е р 13. Симметрирование тензора типа $(1,3)$ по всем нижним индексам:

$$\beta^i_{(jkl)} = \tfrac{1}{6}(\beta^i_{jkl}+\beta^i_{ljk}+\beta^i_{klj}+\beta^i_{kjl}+\beta^i_{lkj}+\beta^i_{jlk}). \tag{8}$$

Снова рассмотрим тензор $\mathbf{A}$ типа $(p,q)$, где $p\geqslant s\geqslant 2$. Выберем группу из $s$ верхних индексов, и пронумеруем выбранные индексы числами $1,\ldots,s$. Тогда каждому тензору, получаемому из $\mathbf{A}$ транспонированием по этим индексам, будет сопоставлена некоторая перестановка $i_1,\ldots,i_s$ номеров $1,\ldots,s$.

---
**стр. 390**

---

Обозначим через $N(i_1,\ldots,i_s)$ число нарушений порядка в ней (см. п. 6 § 4 гл. V). Напомним, что перестановка называется четной, если число нарушений порядка в ней четное, и нечетной в противном случае.

Транспонируя $\mathbf{A}$ по выбранным индексам, мы, как и выше, получаем $s!$ тензоров. Сложим все эти тензоры, предварительно умножив каждый из них на $(-1)^{N(i_1,\ldots,i_s)}$, где $i_1,\ldots,i_s$ — перестановка, ему соответствующая. Сумму разделим на число $s!$. Так построенный тензор называется результатом *альтернирования* тензора $\mathbf{A}$ по выбранным индексам. Его компоненты обозначаются заключением в квадратные скобки тех индексов, по которым производится альтернирование.

П р и м е р 14. Альтернирование тензора типа $(3,0)$ по первому и третьему индексам:

$$\alpha^{[i|j|k]} = \tfrac{1}{2}(\alpha^{ijk}-\alpha^{kji}).$$

П р и м е р 15. Альтернирование тензора типа $(1,3)$ по всем нижним индексам:

$$\beta^i_{[jkl]} = \tfrac{1}{6}(\beta^i_{jkl}+\beta^i_{ljk}+\beta^i_{klj}-\beta^i_{kjl}-\beta^i_{lkj}-\beta^i_{jlk}). \tag{9}$$

П р и м е р 16. В § 4 гл. VI мы отмечали, что детерминант матрицы линейного преобразования является инвариантом. Выразим этот инвариант при помощи тензорных операций. Пусть $\alpha^i_j$ — элементы матрицы $A$ преобразования в некотором базисе $\mathbf{e}$. Тогда $n$-кратное произведение $\mathbf{A}$ на само себя $\mathbf{A}\otimes\cdots\otimes\mathbf{A}$ имеет компоненты $\alpha^{i_1}_{j_1}\alpha^{i_2}_{j_2}\ldots\alpha^{i_n}_{j_n}$. Альтернируем это произведение по всем нижним индексам, а затем свернем по всем индексам. Мы получим инвариант

$$\Delta = \alpha^{i_1}_{[j_1}\alpha^{i_2}_{j_2}\ldots\alpha^{i_n}_{j_n]}.$$

Докажем, что это и есть интересующий нас детерминант.

Здесь $n$ индексов суммирования, каждый из которых принимает $n$ значений. Следовательно, правая часть распадается на $n^n$ слагаемых. Каждое из этих слагаемых представляет собой сумму $n!$ членов, возникающих при альтернировании. Если в наборе значений индексов суммирования, определяющих

---
**стр. 391**

---

какое-то слагаемое, есть два одинаковых, то такое слагаемое равно нулю. Действительно, для каждого члена в нем, взятого со знаком плюс, найдется не отличающийся член, взятый со знаком минус. Пусть все значения индексов суммирования, определяющие слагаемое, различны. Тогда, переставляя сомножители в каждом члене такого слагаемого, упорядочим верхние индексы и этим приведем его к виду

$$\alpha^1_{[1}\ldots\alpha^n_{n]} = \frac{1}{n!}\sum_{(k_1,\ldots,k_n)}(-1)^{N(k_1,\ldots,k_n)}\alpha^1_{k_1}\ldots\alpha^n_{k_n}.$$

Всего слагаемых такого вида $n!$. Следовательно,

$$\Delta = n!\,\alpha^1_{[1}\ldots\alpha^n_{n]}.$$

Отсюда по формуле полного разложения детерминанта $\Delta=\det A$.

Если разбор этого примера вызвал затруднения, выпишите подробно всю сумму при $n=2$.

**9. Замечание.** Пусть имеется какое-то соотношение между тензорами, написанное при помощи введенных нами тензорных операций. Если выбран базис, это соотношение порождает такие же соотношения между компонентами рассматриваемых тензоров. Тензорные операции инвариантны в том смысле, что соотношения между компонентами выглядят одинаково, каков бы ни был базис. Скажем, соотношение $\mathbf{A}=x\otimes y+z\otimes z$, где $x$, $y$ и $z$ — векторы, равносильно равенству $\alpha^{ij}=\xi^i\eta^j+\zeta^i\zeta^j$ между компонентами, причем безразлично, в каком базисе, так как во всех базисах оно выглядит одинаково.

В силу этого обстоятельства часто, говоря о тензорах, имеют в виду их компоненты или наоборот, говоря о компонентах, имеют ввиду тензоры. Говорят, например, «тензор $\alpha_{ijk}$» вместо «тензор, компоненты которого в таком-то базисе равны $\alpha_{ijk}$». Это не может вызвать недоразумений и сильно упрощает речь. В дальнейшем мы будем пользоваться подобными сокращениями.

---
**стр. 392**

---

**10. Симметричные и антисимметричные тензоры.**

О п р е д е л е н и е. Тензор называется *симметричным по паре индексов*, если он не меняется при транспонировании по этой паре. Результат его альтернирования по этой паре равен нулевому тензору.

Тензор *симметричен по группе индексов*, если он симметричен по любой паре индексов из этой группы. В таком случае он не меняется при любом транспонировании по индексам этой группы.

Не представляет труда убедиться, что результат симметрирования тензора по некоторой группе индексов является тензором, симметричным по этим индексам. Если, например, переставить любые два нижних индекса в формуле (8), то ее правая часть изменится только порядок слагаемых.

О п р е д е л е н и е. Тензор называется *антисимметричным по паре индексов*, если он меняет знак на $(-1)$ при транспонировании по этой паре индексов, или иначе говоря, результат его симметрирования по ней равен нулевому тензору.

Если тензор антисимметричен по паре индексов, то равны нулю те его компоненты, у которых совпадают значения этих индексов. Это видно из того, что каждый слой, соответствующий этим индексам, — антисимметричная квадратная матрица.

Тензор *антисимметричен по группе индексов*, если он антисимметричен по любой паре индексов из этой группы.

Результат альтернирования тензора по нескольким индексам антисимметричен по этим индексам. Причину этого легко понять, если переставить какие-нибудь два нижних индекса в формуле (9): ее правая часть изменит только знак.

П р е д л о ж е н и е 7. *Антисимметричный по группе из $s$ индексов тензор не меняется при транспонировании по ней, если соответствующая перестановка индексов четная, и умножается на $(-1)$, если нечетная.*

Д о к а з а т е л ь с т в о. Напомним, что перестановка $i_1,i_2,\ldots,i_s$ называется четной, если число нарушений порядка в ней $N(i_1,i_2,\ldots,i_s)$ — четное число, и называется нечетной в противном случае. Транспонирование, соответствующее перестановке индексов $i_1,\ldots,i_s$, сводится к последовательной

---
**стр. 393**

---

перестановке пар индексов. Поскольку каждая из них меняет знак всех компонент тензора, достаточно доказать, что данное транспонирование осуществимо за $N(i_1,\ldots,i_s)$ перестановок пар индексов.

Последнее утверждение равносильно тому, что числа $1,\ldots,s$ можно расположить в порядке $i_1,\ldots,i_s$, переставляя $N(i_1,\ldots,i_s)$ раз соседние числа. Докажем сначала, что числа $i_1,\ldots,i_s$ можно указанным способом расположить в порядке возрастания. Для этого отыщем в перестановке $i_1,\ldots,i_s$ число 1 и переставим его на первое место, меняя местами последовательно со всеми числами, стоящими левее. Все они больше 1, и мы переставим единицу столько раз, в скольких нарушениях порядка она виновна. Затем отыщем число 2 и точно так же переставим его на второе место. При этом придется переставить со всеми числами, которые стоят левее него, кроме 1, а со всеми ними оно образует нарушение порядка.

Проделаем далее то же самое со всеми числами $3,\ldots,(s-1)$. Число $s$ окажется на последнем месте, переставлять его не надо, но и нарушений порядка в перестановку оно не вносит. В результате будет сделано $N(i_1,\ldots,i_s)$ попарных перестановок чисел, и числа окажутся расположенными в порядке возрастания.

Теперь, исходя из $1,\ldots,s$, мы можем проделать те же перестановки чисел в обратном порядке и получить $i_1,\ldots,i_s$. На это потребуется также $N(i_1,\ldots,i_s)$ перестановок соседних чисел.

П р е д л о ж е н и е 8. *Если тензор симметричен по группе из $s$ индексов, то результат его альтернирования по этой группе индексов — нулевой тензор.*

*Если тензор антисимметричен по группе индексов, то результат его симметрирования по ней — нулевой тензор.*

Обе части предложения доказываются одинаково. Докажем первую. Все $s!$ тензоров, которые можно получить транспонированием, одинаковы. При альтернировании мы складываем их со знаками, определяемыми четностями соответствующих перестановок. При этом все слагаемые уничтожатся, так как из $s!$ перестановок ровно половина четных,

---
**стр. 394**

---

а половина нечетных. Действительно, меняя местами два первых числа в перестановке, мы изменяем ее четность (вводится или ликвидируется ровно одно нарушение порядка). Этим устанавливается взаимно однозначное соответствие между четными и нечетными перестановками.

З а м е ч а н и е. Если индексов больше двух, равенства нулю результата альтернирования (симметрирования) по этим индексам еще не достаточно для того, чтобы тензор был симметричным (антисимметричным) по ним.

### Упражнения

1. Пусть $\mathcal{B}$ — линейное пространство билинейных функций, определенных на линейном пространстве $\mathcal{L}$, а $\mathbf{A}$ — линейное преобразование пространства $\mathcal{B}$. Докажите, что $\mathbf{A}$ — тензор типа $(2,2)$ в пространстве $\mathcal{L}$.

2. а) Сколько компонент имеет трехвалентный тензор в четырехмерном пространстве? б) Сколько слагаемых содержит выражение какой-либо его компоненты в новом базисе через компоненты в старом базисе?

3. Тензор типа $(0,n)$ в $n$-мерном линейном пространстве в базисе $\mathbf{e}$ имеет компоненты $\varepsilon_{i_1\ldots i_n}=0$, если среди значений $i_1,\ldots,i_n$ есть одинаковые, и

$$\varepsilon_{i_1\ldots i_n}=(-1)^{N(i_1,\ldots,i_n)}$$

в противном случае. Найдите компоненты этого тензора в базисе $\mathbf{e}'=\mathbf{e}S$.

4. Линейная функция $\mathsf{f}$ задана в базисе $\mathbf{e}$ строкой $\boldsymbol{\varphi}$, а вектор $a$ — столбцом $\boldsymbol{\alpha}$. Найдите матрицу тензора $a\otimes\mathsf{f}$. Какой геометрический смысл имеет этот тензор?

5. Сколько различных тензоров можно образовать при помощи свертывания из тензора типа $(2,2)$?

6. Докажите, что тензор из задачи 3 антисимметричен по любому подмножеству множества индексов.

7. Докажите, что для любого тензора типа $(1,1)$ выполнены равенства

$$a_j^{(i}a_l^{k)}=a_{(j}^ia_l^{k)}, \qquad a_j^{[i}a_l^{k]}=a_{[j}^ia_l^{k]}.$$

Решения: [[../reshebnik_beklemishev/rbek_g09_s01|Решебник, §1]].
