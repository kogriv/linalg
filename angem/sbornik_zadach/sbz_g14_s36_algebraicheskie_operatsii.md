# Глава 14. Тензоры

## § 36. Алгебраические операции с тензорами

### Линейные операции, умножение тензоров (36.1–36.20)

**36.1.** Проверить закон преобразования компонент суммы тензоров $a^i_{jk}$ и $b^i_{jk}$, исходя из закона преобразования компонент слагаемых.

**36.2.** Как связана сумма линейных преобразований с суммой соответствующих тензоров?

**36.3.** Пусть $A_{\mathbf{e}}$ — матрица линейного преобразования $\varphi$, $B_{\mathbf{e}}$ — матрица билинейной функции $\mathrm{b}$ в базисе $\mathbf{e}$. Определена ли сумма $A_{\mathbf{e}}+B_{\mathbf{e}}$? Будет ли тензором соответствие, сопоставляющее каждому базису $\mathbf{e}$ сумму матриц $A_{\mathbf{e}}+B_{\mathbf{e}}$?

**36.4.** Тензоры $a$ и $b$ одного типа имеют в базисе $\mathbf{e}$ матрицы компонент $A$ и $B$. Найти компоненты тензоров: а) $a+b$; б) $2a+3b$; в) $b-2a$ в том же базисе, если:
1) $A=A_{650}$, $B=A_{651}$; 2) $A=A_{651}$, $B=A_{652}$;
3) $A=A_{652}$, $B=A_{653}$; 4) $A=A_{693}$, $B=A_{697}$.

**36.5.** Заданы матрицы $A,B,C,D$ из компонент четырех тензоров. Определить, являются ли тензоры линейно зависимыми, если:
1) $A=A_{662}$, $B=A_{665}$, $C=A_{663}$, $D=A_{652}$;
2) $A=A_{650}$, $B=A_{651}$, $C=A_{653}$;
3) $A=A_{666}$, $B=A_{663}$, $C=A_{650}$, $D=A_{653}$.

**36.6.** 1) Какова размерность линейного пространства $\mathcal{L}$ тензоров типа $(p,q)$ в двумерном пространстве?

---
**стр. 335**

---

2) Указать какой-нибудь базис в $\mathcal{L}$.
3) Указать еще один базис в $\mathcal{L}$.

**36.7.** Базису $\mathbf{e}$ двумерного линейного пространства соответствует базис $\mathbf{e}^*$ в пространстве тензоров типа:
1) $(0,1)$; 2) $(1,1)$; 3) **(р)** $(0,2)$; 4) $(1,2)$.
Базис $\mathbf{e}^*$ состоит из тензоров, имеющих в базисе $\mathbf{e}$ одну компоненту, равную 1, а остальные — равные 0. Как преобразуется базис $\mathbf{e}^*$, если базис $\mathbf{e}$ преобразуется матрицей перехода $S$? → см. [[sbz_g14_resheniya#36.7 (р)|Решения к Главе 14]] *(пометка «(р)» относится к пункту 3)*

**36.8.** Проверить закон преобразования компонент тензора $a\otimes b$, исходя из законов преобразования компонент сомножителей $a^i_{jk}$, $b^i_{jk}$.

**36.9.** Найти тип и матрицу тензора $a\otimes b$, если:

| | тип $a$ | матрица $a$ | тип $b$ | матрица $b$ |
|---|---|---|---|---|
| 1) | $(1,0)$ | $\mathbf{c}_{12}$ | $(1,0)$ | $\mathbf{c}_{31}$ |
| 2) | $(1,0)$ | $\mathbf{c}_{12}$ | $(0,1)$ | $\mathbf{c}_{31}^T$ |
| 3) | $(1,0)$ | $\mathbf{c}_{12}^T$ | $(1,0)$ | $\mathbf{c}_{31}$ |
| 4) | $(0,1)$ | $\mathbf{c}_{12}$ | $(0,1)$ | $\mathbf{c}_{31}^T$ |
| 5) | $(0,2)$ | $A_{17}$ | $(0,1)$ | $\mathbf{c}_7^T$ |
| 6) | $(0,1)$ | $\mathbf{c}_7^T$ | $(0,2)$ | $A_{17}$ |
| 7) | $(2,0)$ | $A_{18}$ | $(1,0)$ | $\mathbf{c}_8$ |
| 8) | $(1,1)$ | $A_{18}$ | $(1,0)$ | $\mathbf{c}_8$ |
| 9) | $(1,0)$ | $\mathbf{c}_8$ | $(2,0)$ | $A_{18}$ |
| 10) | $(1,0)$ | $\mathbf{c}_8$ | $(1,1)$ | $A_{18}$ |
| 11) | $(0,3)$ | $A_{650}$ | $(0,1)$ | $\mathbf{c}_{25}^T$ |
| 12) | $(0,1)$ | $\mathbf{c}_{25}^T$ | $(0,3)$ | $A_{650}$ |
| 13) | $(1,2)$ | $A_{651}$ | $(0,1)$ | $\mathbf{c}_8^T$ |
| 14) | $(0,1)$ | $\mathbf{c}_8^T$ | $(1,2)$ | $A_{651}$ |
| 15) | $(0,2)$ | $A_{17}$ | $(0,2)$ | $A_{18}$ |
| 16) | $(1,1)$ | $A_{13}$ | $(0,2)$ | $A_{18}$ |
| 17) | $(1,1)$ | $A_{13}$ | $(1,1)$ | $A_{19}$ |
| 18) | $(2,0)$ | $A_{13}$ | $(1,1)$ | $A_{19}$ |

**36.10.** Записать матрицу из компонент тензора:
1) $a^ib^j$; 2) $a_ib_j$; 3) $a^ib_j$; 4) $a_ib^j$
как кронекеровское произведение матриц из компонент этих тензоров.

**36.11.** Пусть $a,b$ — двухвалентные тензоры с матрицами $A,B$. Какого типа должны быть эти тензоры, чтобы матрица их тензорного произведения была (правым) кронекеровским произведением:
1) $A\otimes B$; 2) $B\otimes A$?

---
**стр. 336**

---

**36.12.** Линейные функции $\mathrm{f}$ и $\mathrm{g}$ заданы в базисе $\mathbf{e}$ координатными строками $\varkappa$ и $\mu$. Найти матрицу тензора: 1) $\mathrm{f}\otimes\mathrm{g}$; 2) $\mathrm{g}\otimes\mathrm{f}$.
Какой геометрический смысл имеют эти тензоры?

**36.13.** Линейная функция $\mathrm{f}$ задана в базисе $\mathbf{e}$ координатной строкой $\varkappa$, вектор $y$ — столбцом $\eta$. Найти матрицу тензора $\mathrm{f}\otimes y$. Какой геометрический смысл имеет этот тензор?

**36.14.** 1) Пусть $x$ — вектор, $\mathrm{f}$ — ковектор. Доказать, что $\mathrm{f}\otimes x=x\otimes\mathrm{f}$.
2) Привести пример тензоров $a$ и $b$, для которых $a\otimes b\neq b\otimes a$.

**36.15.** Пусть $x_1,x_2,x_3$ — векторы, а $\mathrm{f}_1,\mathrm{f}_2,\mathrm{f}_3$ — ковекторы. Какие из приведенных ниже выражений имеют смысл? Если данное выражение есть тензор, указать его тип:
1) $x_1\otimes x_2+x_3\otimes x_3$; 2) $x_1\otimes x_2\otimes x_3+x_2\otimes x_3$;
3) $x_1\otimes\mathrm{f}_1-2\mathrm{f}_1\otimes x_1$; 4) $x_1\otimes\mathrm{f}_2+\mathrm{f}_1\otimes x_2$;
5) $x_1\otimes\mathrm{f}_2+x_2\otimes\mathrm{f}_1$; 6) $\mathrm{f}_1\otimes x_1\otimes x_2+x_2\otimes x_3\otimes\mathrm{f}_2$;
7) $x_1\otimes x_2+x_3\otimes x_3-x_1\otimes x_1$; 8) $\mathrm{f}_1\otimes\mathrm{f}_2-3(\mathrm{f}_2\otimes\mathrm{f}_3)$.

**36.16.** Найти компоненты тензоров 1), 3), 5), 7), 8) задачи 36.15, если векторы $x_1,x_2,x_3$ и ковекторы $\mathrm{f}_1,\mathrm{f}_2,\mathrm{f}_3$ заданы с помощью столбцов и строк соответственно: $\mathbf{c}_{20}$, $\mathbf{c}_{12}$, $\mathbf{c}_{25}$, $\mathbf{c}_{10}^T$, $\mathbf{c}_{22}^T$.

**36.17.** 1) Пусть $a=x\otimes y$, а векторы $x$ и $y$ имеют компоненты 1, 0, 0 и 0, 1, 0 соответственно. Найти компоненты тензора $a$ в базисе $\mathbf{e}$ и в базисе $\mathbf{e}'=\mathbf{e}S$, где $S=A_{207}$.
2) Пусть $a=\mathrm{f}\otimes\mathrm{g}$, а ковекторы $\mathrm{f}$ и $\mathrm{g}$ имеют в базисе $\mathbf{e}$ координатные строки $(1,0,0)$ и $(0,1,0)$ соответственно. Найти компоненты тензора $a$ в базисе $\mathbf{e}$ и в базисе $\mathbf{e}'=\mathbf{e}S$, где $S=A_{207}$.
3) Пусть $a=x\otimes\mathrm{f}$, а вектор $x$ и ковектор $\mathrm{f}$ имеют в базисе $\mathbf{e}$ координаты 1, 0, 0 и 0, 1, 0 соответственно. Найти компоненты тензора $a$ в базисе $\mathbf{e}$ и в базисе $\mathbf{e}'=\mathbf{e}S$, где $S=A_{207}$.
Сравнить результаты задач 1), 2), 3).

**36.18.** Разложить тензор в произведение одновалентных тензоров, если он имеет:
1) тип $(2,0)$ и матрицу $A_5$;
2) тип $(2,1)$ и матрицу $A_{673}$.

**36.19.** 1) Пусть $a$ — тензор типа $(1,1)$ и матрица его компонент имеет ранг $r$. Доказать, что найдутся $r$ линейно независимых

---
**стр. 337**

---

векторов $a_1,\ldots,a_r$ и $r$ линейно независимых ковекторов $\mathrm{f}_1,\ldots,\mathrm{f}_r$ таких, что $a=\sum\limits_{\alpha=1}^r a_\alpha\otimes\mathrm{f}^\alpha$.
2) Сформулировать и доказать аналогичное утверждение для тензоров типа $(2,0)$.

**36.20.** 1) Пусть тензор $a$ типа $(0,2)$ имеет в некотором базисе матрицу ранга $r$. Доказать, что существуют $r$ линейно независимых ковекторов $\mathrm{f}_1,\ldots,\mathrm{f}_r$ и $r$ линейно независимых ковекторов $\mathrm{g}_1,\ldots,\mathrm{g}_r$ таких, что $a=\sum\limits_{\alpha=1}^r \mathrm{f}_\alpha\otimes\mathrm{g}_\alpha$.
2) Представить билинейную функцию $3\xi^1\eta^1+2\xi^1\eta^2+3\xi^2\eta^1+2\xi^2\eta^2$ как произведение линейных. Единственно ли такое представление?
3) Билинейная функция $\mathrm{f}$ в некотором базисе линейного пространства задана матрицей $A_{454}$. Представить ее как сумму двух произведений пар линейных функций: $\mathrm{f}(x,y)=\mathrm{f}_1(x)\mathrm{g}_1(y)+\mathrm{f}_2(x)\mathrm{g}_2(y)$. Единственно ли это представление?

### Свертывание (36.21–36.29)

**36.21.** Исходя из законов преобразования тензоров $a^i_{jk}$, $a^i_j$, $b^i_j$, $a_i$, $b^{ij}$, $a^{ij}_{klm}$, $\xi^k$, проверить закон преобразования компонент сверток:
1) $a^i_{ji}$; 2) $a^i_jb^j_i$; 3) $a_ib^{ij}$; 4) $a^{ij}_{klm}\xi^k$.

**36.22.** Исходя из геометрического смысла тензоров $a_i$, $\xi^i$, $a^i_j$, $b_{ij}$, объяснить геометрический смысл сверток:
1) $a_i\xi^i$; 2) $a^i_j\xi^j$; 3) $b_{ij}\xi^i\xi^j$.

**36.23.** Можно ли свернуть:
1) вектор и вектор? 2) вектор и ковектор?
3) пару ковекторов?

**36.24.** Записать произведение линейных преобразований в тензорных обозначениях.

**36.25.** Тензоры $a^i_j$, $\xi^i$, $\varkappa_i$ заданы матрицами: $A_{232}$, $\mathbf{c}_{104}$, $\mathbf{c}_{104}^T$. Вычислить свертки:
1) $a^i_j\xi^j$; 2) $a^i_j\varkappa_i$; 3) $\varkappa_j\xi^j$.

**36.26.** Сколько различных тензоров можно образовать при помощи свертывания из данного тензора типа $(2,2)$?

**36.27.** Тензор $a^{ij}_k$ задан матрицей:
1) $A_{651}$; 2) $A_{655}$.
Найти матрицы сверток: а) $a^{ij}_i$; б) $a^{ij}_j$.

---
**стр. 338**

---

**36.28.** Тензор $a^{ij}_{kl}$ задан матрицей:
1) $A_{649}$; 2) $A_{693}$; 3) $A_{694}$.
Вычислить свертки: а) $a^{ij}_{il}$; б) $a^{ij}_{kj}$; в) $a^{ij}_{ki}$; г) $a^{ij}_{il}$; д) $a^{ij}_{ij}$; е) $a^{ij}_{ji}$.

> [!note] Пункты а) и г) в оригинале дословно совпадают ($a^{ij}_{il}$) — по-видимому, опечатка в источнике (г), вероятно, задумывался как другая свертка), транскрибировано как есть.

**36.29.** 1) Каждому базису пространства $\mathcal{L}_n$ сопоставлен упорядоченный набор чисел $a^{ij}_{klm}$ (все индексы пробегают значения от 1 до $n$). Известно, что для произвольного вектора $\xi^k$ числа $a^{ij}_{klm}\xi^k$ являются компонентами тензора типа $(2,2)$. Доказать, что $a^{ij}_{klm}$ — тензор типа $(2,3)$.
2) Каждому базису пространства $\mathcal{L}_n$ сопоставлен упорядоченный набор чисел $a^{ij}_{klm}$ (все индексы пробегают значения от 1 до $n$). Известно, что для произвольного тензора $u^k_{ij}$ типа $(1,2)$ числа $a^{ij}_{klm}u^k_{ij}$ являются компонентами тензора типа $(0,2)$. Доказать, что $a^{ij}_{klm}$ — тензор типа $(2,3)$.

### Транспонирование, симметрирование, альтернирование. Симметричные и антисимметричные тензоры (36.30–36.57)

**36.30.** Можно ли транспонировать тензор:
1) типа $(1,1)$; 2) типа $(2,0)$?

**36.31.** Один тензор типа $(0,2)$ получается из другого транспонированием. Как связаны соответствующие билинейные функции?

**36.32.** Тензоры
1) $a_{ij}$; 2) $a^{ij}$; 3) $a^i_k$; 4) $a^i_{jk}$
заданы соответственно матрицами $A_{16}$, $A_{16}$, $A_{670}$, $A_{670}$. Найти матрицы транспонированных тензоров.

**36.33.** 1) Сколько различных тензоров можно получить с помощью операции транспонирования из данного тензора $a_{i_1\ldots i_k}$?
2) Тензор типа $(0,3)$ задан матрицей $A_{670}$. Выписать все тензоры, получаемые из него транспонированием. Изменится ли ответ, если данный тензор имеет тип $(3,0)$?
3) Тензор $a$ с компонентами $a_{ijk}$ задан матрицей $A_{727}$. Выписать матрицы транспонированных тензоров $b$ и $c$, если $b_{ijk}=a_{jki}$, $c_{ijk}=a_{ikj}$.
4) Тензор $a$ с компонентами $a_{ijkl}$ задан матрицей $A_{717}$. Выписать матрицы транспонированных тензоров $b$ и $c$, если $b_{ijkl}=a_{kjli}$, $c_{ijkl}=a_{lkji}$.

---
**стр. 339**

---

**36.34.** Пусть $a,b$ — тензоры типа $(1,1)$. Выразить тензор $c=b\otimes a$ через $d=a\otimes b$.

**36.35.** Не используя сокращенных обозначений, выпишите все компоненты тензоров, заданных в пространстве $\mathcal{L}_2$:
1) $x^iy^k$; 2) $x^{(i}y^{k)}$; 3) $x^{[i}y^{k]}$; 4) $x^ia_{jk}$;
5) $x^ia_{ik}$; 6) $x^ka^i_i$; 7) $x^{(k}a^{i)}_i$; 8) $x^{[k}a^{i]}_i$;
9) $a^i_ia^k_k$; 10) $a^i_{[i}a^k_{k]}$; 11) $a^i_{(i}a^k_{k)}$; 12) $\delta^i_ja^k_i$;
13) $\delta^i_ja^j_k$; 14) $\delta^i_ja^j_i$; 15) $\delta^i_ja^k_l$; 16) $a^k_l\delta^i_j$.

**36.36.** Тензор $a^{ij}$ задан матрицей:
1) $A_{10}$; 2) $A_{77}$; 3) $A_{24}$; 4) $A_{232}$.
Найти компоненты тензоров: а) $a^{(ij)}$; б) $a^{[ij]}$.

**36.37.** Тензор $a_{ijk}$ задан матрицей:
1) $A_{650}$; 2) $A_{651}$; 3) $A_{720}$.
Найти компоненты тензоров: а) $a_{(ij)k}$; б) $a_{i(jk)}$; в) $a_{(i|j|k)}$; г) $a_{(ijk)}$.

**36.38.** Тензор $a^{ij}_{kl}$ задан матрицей:
1) $A_{694}$; 2) $A_{684}$.
Найти компоненты тензоров: а) $a^{(ij)}_{kl}$; б) $a^{ij}_{(kl)}$; в) $a^{(ij)}_{(kl)}$.

**36.39.** Тензор $a_{ijk}$ задан матрицей:
1) $A_{650}$; 2) $A_{651}$; 3) $A_{720}$.
Найти компоненты тензоров: а) $a_{[ij]k}$; б) $a_{i[jk]}$; в) $a_{[i|j|k]}$.

**36.40.** Тензор $a^{ij}_{kl}$ задан матрицей:
1) $A_{694}$; 2) $A_{684}$.
Найти компоненты тензоров: а) $a^{[ij]}_{kl}$; б) $a^{ij}_{[kl]}$; в) $a^{[ij]}_{[kl]}$.

**36.41.** Тензор $a_{ijk}$ задан матрицей:
1) $A_{726}$; 2) $A_{721}$.
Найти компоненты тензоров: а) $a_{[ijk]}$; б) $a_{(ijk)}$.

**36.42.** Тензор типа $(0,3)$ задан матрицей $A$:
1) $A_{723}$; 2) $A_{725}$; 3) $A_{720}$; 4) $A_{650}$; 5) $A_{722}$.
Выяснить, является ли тензор симметричным (антисимметричным), и если да, то по каким индексам.

**36.43.** Тензор $a^i_j$ задан матрицей:
1) $A_{58}$; 2) $A_{207}$.
Вычислить инварианты: а) $a^i_i$; б) $a^i_{[i}a^k_{k]}$; в) $a^i_{[i}a^j_ja^k_{k]}$. Сравнить найденные инварианты с коэффициентами характеристического многочлена матрицы $A$.

---
**стр. 340**

---

**36.44.** 1) Доказать, что тензор $\varepsilon_{i_1\ldots i_n}$ (см. задачу 35.21) кососимметричен по любой паре индексов.
2) Доказать, что тензор $\varepsilon_{i_1\ldots i_n}$ кососимметричен по любому подмножеству индексов.
3) Доказать, что тензор $\delta^{i_1\ldots i_k}_{j_1\ldots j_k}$ (см. задачу 35.20) кососимметричен по любой паре верхних индексов.
4) Доказать, что тензор $\delta^{i_1\ldots i_k}_{j_1\ldots j_k}$ кососимметричен по любому подмножеству верхних индексов.
5) Доказать утверждение 4) для нижних индексов.

**36.45.** Пусть $a_{ij}$ и $b^{ij}$ — компоненты соответственно симметричного и антисимметричного тензоров. Вычислить свертку $a_{ij}b^{ij}$.

**36.46.** Для тензора $\delta^{i_1\ldots i_k}_{j_1\ldots j_k}$, определенного в задаче 35.20, и произвольных тензоров $a^{j_1\ldots j_k}$ и $b_{i_1\ldots i_k}$ доказать, что:
1) $\delta^{i_1\ldots i_k}_{j_1\ldots j_k}a^{j_1\ldots j_k}=a^{[i_1\ldots i_k]}$; 2) $\delta^{i_1\ldots i_k}_{j_1\ldots j_k}b_{i_1\ldots i_k}=b_{[j_1\ldots j_k]}$.

**36.47.** Пусть $a_{ijk}\xi^i\xi^j\xi^k=0$ для любого вектора $\xi^i$. Доказать, что $a_{[ijk]}=0$.

**36.48.** Доказать, что $a^{(i}_ja^{k)}_l=a^i_{(j}a^k_{l)}$, $a^{[i}_ja^{k]}_l=a^i_{[j}a^k_{l]}$.

**36.49.** Вычислить:
1) $\delta^i_j\delta^j_l\delta^l_k\delta^k_i$; 2) $\delta^i_j\delta^j_k\delta^k_l\delta^l_m$; 3) $\delta^i_{[i}\delta^j_j\delta^k_{k]}$;
4) $\delta^i_{(i}\delta^j_j\delta^k_{k)}$; 5) $\delta^i_j\delta^j_i\delta^k_la^l_k$.

**36.50.** 1) Пусть тензор симметричен по некоторой паре индексов. Доказать, что операция симметрирования по этим индексам его не меняет, а операция альтернирования дает нулевой тензор.
2) Пусть тензор антисимметричен по некоторой паре индексов. Доказать, что операция симметрирования по этим индексам дает нулевой тензор, а операция альтернирования тензора не меняет.

**36.51.** 1) Доказать, что для симметричного по двум первым индексам тензора имеет место тождество
$$a_{(ijk)}=\frac13(a_{ijk}+a_{kij}+a_{jki}).$$
2) Доказать, что для антисимметричного по двум первым индексам тензора имеет место тождество
$$a_{[ijk]}=\frac13(a_{ijk}+a_{kij}+a_{jki}).$$

---
**стр. 341**

---

**36.52.** 1) Тензор типа $(0,3)$ симметричен по двум первым и симметричен по двум последним индексам. Доказать, что он симметричен также по первому и третьему индексами.
2) Тензор типа $(0,3)$ антисимметричен по двум первым и антисимметричен по двум последним индексам. Доказать, что он антисимметричен также по первому и третьему индексами.
3) Пусть тензор типа $(0,3)$ симметричен по двум первым индексам и антисимметричен по двум последним индексам. Доказать, что он нулевой.

**36.53.** 1) Привести пример тензора типа $(0,3)$, для которого $a_{[ijk]}=0$, но не симметричного по трем индексам.
2) Привести пример тензора типа $(0,3)$, для которого $a_{(ijk)}=0$, но не антисимметричного по трем индексам.
3) Доказать, что для ненулевого тензора $a$ типа $(0,3)$ невозможно одновременное выполнение равенств $a_{(ijk)}=0$ и $a_{[ijk]}=0$.

**36.54.** Доказать, что любой тензор типа $(0,2)$ или $(2,0)$ можно разложить в сумму симметричного и антисимметричного тензоров.

**36.55.** Разложить в сумму симметричного и антисимметричного тензоров тензор типа $(0,2)$, заданный матрицей:
1) $A_{49}$; 2) $A_{16}$; 3) $A_{234}$.

**36.56.** Из символа Кронекера с помощью тензорных операций получить тензоры:
1) $\delta^{ij}_{kl}$; 2) $\delta^{i_1\ldots i_k}_{j_1\ldots j_k}$ (см. задачи 35.18, 35.20).

**36.57.** 1) Пусть симметричный тензор $a$ типа $(0,2)$ имеет в некотором базисе матрицу ранга $r$. Доказать, что существуют $r$ линейно независимых ковекторов $\mathrm{f}_1,\ldots,\mathrm{f}_r$, таких, что
$$a=\sum_{\alpha=1}^r \mathrm{f}_\alpha\otimes\mathrm{f}_\alpha.$$
2) Сформулировать и доказать обратное утверждение.
3) Квадратичная функция $\varphi$ в $\mathcal{L}_2$ задана матрицей $A_{56}$. Представить ее как сумму квадратов двух линейных функций. Единственно ли это представление?

---

Ссылки на матрицы и столбцы вида $A_k$, $\mathbf{c}_k$ — из «Банка столбцов и матриц» (стр. 465–494 оригинала), не перенесенного в vault.

Назад: [[sbz_g14_s35_opredelenie_tenzora|§35. Определение тензора. Тензорные обозначения, пространственные матрицы]]. Далее: [[sbz_g14_s37_tenzory_v_evklidovom_prostranstve|§37. Тензоры в евклидовом пространстве]] (стр. 341–343).
