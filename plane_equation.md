# Уравнение плоскости и нормаль

Для примера из книги [[streng_slu_64 | Стренг. 2 глава. СЛУ ]]. Как получить уравнение плоскости для двух векторов? Уравнение нормали к этой плоскости? Это одно и то же?

Для того чтобы найти уравнение плоскости, проходящей через два вектора, нам действительно нужно найти вектор, который перпендикулярен этой плоскости. Этот вектор называется **нормалью**.

В вашем примере из книги даны два столбца матрицы $A$:

$$\vec{a_1} = \begin{pmatrix} 1 \\ 5 \\ 2 \end{pmatrix}, \quad \vec{a_2} = \begin{pmatrix} 0 \\ 4 \\ 4 \end{pmatrix}$$

---

## 1. Векторное произведение (Находим нормаль)

Чтобы найти вектор $\vec{n}$, который будет перпендикулярен одновременно и $\vec{a_1}$, и $\vec{a_2}$, используется **векторное произведение** ($\vec{a_1} \times \vec{a_2}$).

Результат векторного произведения — это новый вектор, координаты которого вычисляются через определитель матрицы:

$$\vec{n} = \vec{a_1} \times \vec{a_2} = \begin{vmatrix} \mathbf{i} & \mathbf{j} & \mathbf{k} \\ 1 & 5 & 2 \\ 0 & 4 & 4 \end{vmatrix}$$

Где $\mathbf{i}, \mathbf{j}, \mathbf{k}$ — единичные векторы осей $x, y, z$. Раскроем определитель:

1. Для $\mathbf{i}$: $(5 \cdot 4 - 2 \cdot 4) = 20 - 8 = \mathbf{12}$
    
2. Для $\mathbf{j}$: $-(1 \cdot 4 - 2 \cdot 0) = -(4 - 0) = \mathbf{-4}$
    
3. Для $\mathbf{k}$: $(1 \cdot 4 - 5 \cdot 0) = 4 - 0 = \mathbf{4}$
    

**Итог:** Вектор нормали $\vec{n} = (12, -4, 4)$.

_Совет: Для удобства уравнение можно сократить на 4. Вектор $(3, -1, 1)$ будет иметь то же направление._

---

## 2. Уравнение плоскости

Уравнение плоскости и уравнение нормали неразрывно связаны. Общее уравнение плоскости в трехмерном пространстве выглядит так:

$$Ax + By + Cz + D = 0$$

Коэффициенты $A, B, C$ — это и есть координаты нашего вектора нормали $\vec{n} = (A, B, C)$.

Поскольку наша плоскость (пространство столбцов) проходит через начало координат $(0, 0, 0)$, коэффициент $D$ будет равен нулю.

Подставляем наши значения $(3, -1, 1)$:

$$3x - 1y + 1z = 0$$

Или просто: **$3x - y + z = 0$**.

---

## 3. В чем разница между ними?

Хотя они описывают одно и то же геометрическое положение, математически это разные объекты:

- **Вектор нормали** — это "стрелка", указывающая направление, перпендикулярное плоскости. Его записывают как координаты $(A, B, C)$.
    
- **Уравнение плоскости** — это "фильтр" или условие. Оно говорит: "Любая точка $(x, y, z)$ принадлежит этой плоскости только в том случае, если сумма её координат с нашими весами даст ноль".
    

---
# Пояснение векторного произведения
(его суть и как выводится / определяется формула). Как получается / выводится формула с определителем (разложением по первой строке)

### 1. Суть: Чего мы хотим добиться?

Представьте два вектора $\vec{a}$ и $\vec{b}$. Мы хотим найти третий вектор $\vec{c}$, который будет обладать двумя свойствами:

1. **Перпендикулярность:** Он должен быть перпендикулярен и $\vec{a}$, и $\vec{b}$ (то есть всей плоскости, которую они образуют).
    
2. **Направление:** Определяется по «правилу правой руки» (см. иллюстрацию ниже).
    
3. **Длина:** Равна площади параллелограмма, построенного на векторах $\vec{a}$ и $\vec{b}$.
    
![[right-hand-rule.jpeg | Правило правой руки]]


### 2. Фундамент: Свойства базисных векторов

Чтобы вывести формулу, нам не нужно сразу хвататься за сложные числа. Достаточно знать, как взаимодействуют единичные векторы осей (базис): $\vec{i}$ (ось X), $\vec{j}$ (ось Y), $\vec{k}$ (ось Z).

Векторное произведение вводится через простые геометрические правила:

- Если вектор умножить сам на себя, площади нет, результат — **0** ($\vec{i} \times \vec{i} = 0$).
    
- Умножение перпендикулярных единичных векторов дает третий перпендикулярный ($\vec{i} \times \vec{j} = \vec{k}$).
    
- Если поменять порядок, знак меняется ($\vec{j} \times \vec{i} = -\vec{k}$).
    

**Таблица умножения базисных векторов:**

1. $\vec{i} \times \vec{j} = \vec{k}$
    
2. $\vec{j} \times \vec{k} = \vec{i}$
    
3. $\vec{k} \times \vec{i} = \vec{j}$
    
    _(В обратном порядке — знак минус)_.
    

---

### 3. Вывод формулы (Алгебраическая магия)

Теперь возьмем два произвольных вектора и распишем их по координатам:

$$\vec{a} = a_x\vec{i} + a_y\vec{j} + a_z\vec{k}$$

$$\vec{b} = b_x\vec{i} + b_y\vec{j} + b_z\vec{k}$$

Нам нужно найти их произведение $\vec{a} \times \vec{b}$. Мы просто раскрываем скобки, как в обычной алгебре (каждое слагаемое первой скобки на каждое слагаемое второй). Всего получится 9 слагаемых.

$$\vec{a} \times \vec{b} = (a_x\vec{i} + a_y\vec{j} + a_z\vec{k}) \times (b_x\vec{i} + b_y\vec{j} + b_z\vec{k})$$

**Шаг 1. Раскрываем скобки:**

- $a_x\vec{i} \times b_x\vec{i}$ (это 0, так как $\vec{i} \times \vec{i} = 0$)
    
- $a_x\vec{i} \times b_y\vec{j}$ (это $a_x b_y \vec{k}$)
    
- $a_x\vec{i} \times b_z\vec{k}$ (это $a_x b_z (-\vec{j})$)
    
- ... и так далее для всех 9 комбинаций.
    

Три слагаемых занулятся (где $\vec{i}\times\vec{i}$, $\vec{j}\times\vec{j}$, $\vec{k}\times\vec{k}$). Останется 6 слагаемых.

**Шаг 2. Группируем то, что осталось:**

Соберем все, что при $\vec{i}$, при $\vec{j}$ и при $\vec{k}$:

- При $\vec{i}$: получается из $(\vec{j} \times \vec{k})$ и $(\vec{k} \times \vec{j})$. Это $(a_y b_z - a_z b_y)\vec{i}$.
    
- При $\vec{j}$: получается из $(\vec{k} \times \vec{i})$ и $(\vec{i} \times \vec{k})$. Это $(a_z b_x - a_x b_z)\vec{j}$.
    
    _(Обратите внимание: обычно для определителя выносят минус за скобку, меняя знаки внутри, чтобы получилось $-(a_x b_z - a_z b_x)\vec{j}$)_.
    
- При $\vec{k}$: получается из $(\vec{i} \times \vec{j})$ и $(\vec{j} \times \vec{i})$. Это $(a_x b_y - a_y b_x)\vec{k}$.
    

**Итог вывода:**

$$\vec{a} \times \vec{b} = (a_y b_z - a_z b_y)\vec{i} - (a_x b_z - a_z b_x)\vec{j} + (a_x b_y - a_y b_x)\vec{k}$$

---

### 4. Почему используется определитель?

Если вы посмотрите на страшную формулу выше, ее очень трудно запомнить. Но математики заметили, что эта структура точь-в-точь совпадает с правилом раскрытия определителя матрицы $3 \times 3$ по первой строке!

Запишем формальный определитель:

$$\begin{vmatrix} \mathbf{i} & \mathbf{j} & \mathbf{k} \\ a_x & a_y & a_z \\ b_x & b_y & b_z \end{vmatrix}$$

Давайте раскроем его:

1. Берем $\mathbf{i}$, вычеркиваем строку и столбец. Остается малый определитель (минор):
    
    $\mathbf{i} \cdot (a_y b_z - a_z b_y)$. **Совпало!**
    
2. Берем $\mathbf{j}$, ставим **минус** (правило шахматной доски), вычеркиваем. Остается:
    
    $-\mathbf{j} \cdot (a_x b_z - a_z b_x)$. **Совпало!**
    
3. Берем $\mathbf{k}$, вычеркиваем. Остается:
    
    $\mathbf{k} \cdot (a_x b_y - a_y b_x)$. **Совпало!**
    

**Вывод:**

Формула с определителем — это **мнемоническое правило** (удобный способ запоминания). Сама физика процесса заложена в свойствах пространства (как $\vec{i}$ крутится к $\vec{j}$), а определитель — это просто компактная упаковка результата алгебраического раскрытия скобок.